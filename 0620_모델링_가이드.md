# 0620 모델링 가이드

## 0. 현재 상황 요약

현재 전처리는 모델링 착수가 가능한 수준까지 완료된 상태로 볼 수 있다.

이미 다음 단계가 완료되었다.

1. 격자-일 단위 최종 피처셋 생성
2. 실제 산불 발생 이력 기반 target 라벨 생성
3. 500m / 1km / 2km 라벨 반경별 EDA 데이터셋 생성
4. 자동 EDA 리포트 및 시각화 산출물 생성
5. 500m 기준으로 산불 발생 격자에서 습도는 낮고, DWI / FFDRI / P-FFDRI는 높게 나타나는 방향성 확인

따라서 지금부터는 전처리를 더 붙잡기보다는, 현재 산출물을 기준으로 모델링과 검증을 시작하고, 모델링 결과를 보면서 필요한 부분만 보정하는 전략이 적절하다.

---

# 1. 모델링에 사용할 산출물

## 1.1 핵심 입력 데이터 구조

모델링에 필요한 데이터는 크게 두 종류다.

| 구분 | 경로 | 의미 |
|---|---|---|
| 설명변수 X | `output/final/final_feature_daily/` | 날짜-격자 단위 최종 피처셋 |
| 정답 y | `output/target/target_fire_daily_500m.parquet` 등 | 실제 산불 발생 이력 기반 라벨 |

즉 모델링의 기본 구조는 다음과 같다.

```text
X = output/final/final_feature_daily/
y = output/target/target_fire_daily_500m.parquet
```

여기서 `final_feature_daily`는 특정 날짜의 특정 격자에 대해 기상, 임상, 지형, 전력설비, 산불위험지수 계열 변수를 모두 계산한 데이터다.

한 행은 대략 다음 의미를 가진다.

```text
2022-04-05, grid_id=12345 격자는
DWI가 얼마이고,
FMI가 얼마이고,
TMI 계열 값이 얼마이고,
PEI가 얼마이고,
최종 FFDRI / P-FFDRI가 얼마다.
```

`target_fire_daily_500m.parquet`는 산불 발생 이력을 500m 반경 기준 격자 라벨로 변환한 것이다. 같은 방식으로 1km, 2km 기준 라벨도 존재한다.

---

## 1.2 모델링용으로 우선 사용할 파일

가장 먼저 추천하는 모델링 파일은 다음이다.

```text
output/eda/eda_sample_500m.parquet
```

이 파일은 전체 데이터가 너무 크기 때문에 EDA와 빠른 모델링을 위해 만든 샘플 데이터다. 산불 발생 격자는 전체 포함하고, 비발생 격자는 일부 샘플링한 구조로 보면 된다.

처음부터 `output/final/final_feature_daily/` 전체를 직접 모델링에 쓰면 데이터가 너무 커서 시간이 오래 걸리고, 메모리 문제가 발생할 수 있다. 따라서 남은 시간이 적은 현재 상황에서는 `eda_sample_500m.parquet`로 baseline 모델을 먼저 만드는 것이 현실적이다.

### 추천 순서

| 우선순위 | 데이터 | 목적 |
|---:|---|---|
| 1 | `output/eda/eda_sample_500m.parquet` | 빠른 baseline 모델링 |
| 2 | `output/eda/eda_sample_1km.parquet` | 라벨 반경 민감도 비교 |
| 3 | `output/eda/eda_sample_2km.parquet` | 라벨 반경 민감도 비교 |
| 4 | `output/final/final_feature_daily/` + target join | 최종 전체 데이터 기반 모델링 |

시간이 부족하면 1순위인 500m 샘플만으로도 발표용 baseline 모델은 만들 수 있다.

---

## 1.3 반드시 제외해야 할 컬럼

모델링할 때는 target과 직접 연결된 컬럼, 식별자, 문자열성 메타데이터를 제외해야 한다.

### 반드시 제외

```text
fire_label
fire_count
fire_objt_ids
fire_addresses
fire_amount_sum
event_lon_mean
event_lat_mean
event_x_mean
event_y_mean
target_grid_id
target_grid_size_m
target_grid_x
target_grid_y
```

위 변수들은 실제 산불 발생 결과를 기반으로 생성된 정보이므로 모델 입력에 넣으면 leakage가 발생한다.

### 보통 제외

```text
grid_id
date
city_name
```

단, `date`에서 추출한 `year`, `month`, `dayofyear`, `season` 등은 사용할 수 있다. `grid_id`는 그대로 넣으면 특정 격자를 외우는 모델이 될 수 있으므로 일반적으로 제외하는 것이 안전하다.

`city_name`은 범주형 변수로 쓸 수도 있지만, 발표용 baseline에서는 제외하고 시작하는 것이 간단하다. 이후 지역별 보정 모델을 만들 때 사용할 수 있다.

---

## 1.4 우선 사용할 피처 후보

### 1차 baseline 피처

처음 모델에는 너무 많은 변수를 넣지 말고, 해석 가능한 핵심 변수부터 넣는 것이 좋다.

```text
ta_mean
ta_max
hm_mean
hm_min
effective_humidity
rn_day_mean
rn_day_max
rne
wind_ws_mean
wind_ws_max
dwi
dwi_n
fmi
fmi_n
elevation
slope
aspect_sin
aspect_cos
tmi_base_n
tmi_p
ywi
Da
Ws
Dr
pole_count
pole_n
road_prox
river_far
pei
ffdri
pffdri
day_weight
```

### 피처 그룹별 의미

| 그룹 | 주요 변수 | 의미 |
|---|---|---|
| 기상 | `ta_mean`, `ta_max`, `hm_mean`, `hm_min`, `wind_ws_mean`, `wind_ws_max` | 당일 기상 조건 |
| 누적 건조 | `effective_humidity`, `rne`, `rn_day_mean`, `rn_day_max` | 실효습도와 강수 감쇠 효과 |
| 공식 위험지수 | `dwi`, `fmi`, `tmi_base_n`, `ffdri`, `day_weight` | 공식 산불위험지수 구조 |
| 확장 위험지수 | `ywi`, `Da`, `Ws`, `Dr`, `pei`, `pffdri` | 양간지풍, 전력설비, 접근성 반영 |
| 지형 | `elevation`, `slope`, `aspect_sin`, `aspect_cos` | 지형 조건 |
| 전력/접근성 | `pole_count`, `pole_n`, `road_prox`, `river_far`, `pei` | 전신주 밀도와 접근성 |

---

# 2. 추천 모델

본 프로젝트의 모델링 목표는 단순히 산불 발생 여부를 맞히는 것만이 아니라, 산불 발생 가능성이 높은 격자와 날짜를 우선적으로 선별하는 것이다. 따라서 모델은 다음 세 가지 관점에서 선택하는 것이 적절하다.

1. baseline으로 사용할 수 있는가
2. 대용량 tabular 데이터에서 안정적으로 학습 가능한가
3. 변수 중요도와 결과 해석이 가능한가

현재 데이터는 기상, 임상, 지형, 전력설비, 산불위험지수 등 다양한 정형 feature가 결합된 tabular 데이터이므로, 딥러닝보다는 tree-based boosting 모델을 중심으로 비교하는 것이 현실적이다.

---

## 2.1 가장 먼저 만들 모델: Logistic Regression

### 목적

Logistic Regression은 성능을 최고로 끌어올리기 위한 모델이라기보다, baseline과 해석을 위한 모델이다.

장점은 다음과 같다.

* 빠르다.
* 변수 방향성을 확인하기 쉽다.
* baseline으로 설명하기 좋다.
* 발표에서 “단순 선형 기준 모델”로 사용하기 좋다.
* `DWI`, `FFDRI`, `P-FFDRI`, `effective_humidity` 등 주요 변수의 방향성이 산불 라벨과 맞는지 확인하기 좋다.

### 추천 사용 상황

```text
공식 위험지수와 주요 기상변수만으로 산불 라벨을 어느 정도 구분할 수 있는지 확인
```

### 추천 feature

처음에는 너무 많은 변수를 넣기보다 다음 정도로 시작하는 것이 좋다.

```text
dwi, ffdri, pffdri,
effective_humidity, hm_mean, hm_min,
rn_day_mean, rne,
ta_mean, wind_ws_mean,
fmi, tmi_p, pei
```

### 주의점

스케일 차이가 있으므로 Logistic Regression에는 `StandardScaler`를 적용하는 것이 좋다.

추천 파이프라인:

```python
from sklearn.pipeline import Pipeline
from sklearn.preprocessing import StandardScaler
from sklearn.linear_model import LogisticRegression

model = Pipeline([
    ("scaler", StandardScaler()),
    ("model", LogisticRegression(
        class_weight="balanced",
        max_iter=1000,
        random_state=42
    ))
])
```

### 해석 포인트

Logistic Regression은 최종 모델이라기보다는 다음 질문에 답하기 위한 모델이다.

```text
단순 선형 모델에서도 산불 발생 격자가 어느 정도 구분되는가?
DWI, FFDRI, P-FFDRI의 계수 방향이 양수로 나오는가?
습도 계열 변수는 음수 방향으로 나오는가?
```

---

## 2.2 가장 추천하는 실전 모델: LightGBM

### 목적

현재 프로젝트에서 가장 추천하는 실전 모델은 LightGBM이다.

이유는 다음과 같다.

* 대용량 tabular 데이터에 강하다.
* 학습 속도가 빠르다.
* 결측치 처리에 비교적 유연하다.
* 비선형 관계와 변수 간 상호작용을 잘 잡는다.
* feature importance를 뽑기 쉽다.
* 불균형 데이터에 대응하기 좋다.
* 발표에서 “최종 보정 모델”로 설명하기 좋다.

현재 데이터는 기상, 지형, 임상, 전력설비, 위험지수처럼 다양한 tabular feature를 결합한 구조이므로 LightGBM이 가장 적합하다.

### 추천 사용 상황

```text
최종 baseline 모델
P-FFDRI 보정 모델
고위험 격자 선별 모델
feature importance 분석
500m / 1km / 2km 라벨 비교 모델
```

### 추천 초기 설정

```python
from lightgbm import LGBMClassifier

model = LGBMClassifier(
    objective="binary",
    n_estimators=1000,
    learning_rate=0.03,
    num_leaves=31,
    max_depth=-1,
    subsample=0.8,
    colsample_bytree=0.8,
    class_weight="balanced",
    random_state=42,
    n_jobs=-1
)
```

### 튜닝 우선순위

처음부터 복잡하게 튜닝하기보다 아래 순서로 조정하는 것이 좋다.

| 파라미터                |        추천 범위 | 의미       |
| ------------------- | -----------: | -------- |
| `n_estimators`      |   300 ~ 2000 | 트리 개수    |
| `learning_rate`     |   0.01 ~ 0.1 | 학습률      |
| `num_leaves`        |   15, 31, 63 | 트리 복잡도   |
| `max_depth`         | -1, 5, 7, 10 | 트리 깊이 제한 |
| `subsample`         |    0.7 ~ 1.0 | 행 샘플링    |
| `colsample_bytree`  |    0.7 ~ 1.0 | 컬럼 샘플링   |
| `min_child_samples` |  20, 50, 100 | 과적합 방지   |

### 해석 포인트

LightGBM의 feature importance를 통해 다음을 확인할 수 있다.

```text
DWI, FFDRI, P-FFDRI가 상위 변수에 드는가?
effective_humidity, hm_min 등 건조도 변수가 중요한가?
PEI, pole_count, road_prox 등 전력설비·접근성 변수가 보조 설명력을 가지는가?
YWI, Da 등 양간지풍 계열 변수가 실제로 사용되는가?
```

---

## 2.3 비교 모델: XGBoost

### 목적

XGBoost는 LightGBM과 같은 gradient boosting 계열 모델이며, 성능이 안정적이고 널리 사용되는 모델이다.

장점은 다음과 같다.

* tabular 데이터에서 강력한 성능을 보인다.
* 과적합 제어가 비교적 잘 된다.
* feature importance를 확인할 수 있다.
* 발표에서 LightGBM과 비교 모델로 제시하기 좋다.

### 추천 사용 상황

```text
LightGBM 결과와 비교
boosting 계열 모델 간 성능 안정성 확인
최종 모델 후보 비교
```

### 주의점

데이터가 크면 LightGBM보다 느릴 수 있다. 따라서 전체 원본 데이터보다는 `eda_sample_500m.parquet` 또는 적절히 샘플링된 모델링 데이터에서 먼저 테스트하는 것이 좋다.

### 추천 초기 설정

```python
from xgboost import XGBClassifier

model = XGBClassifier(
    objective="binary:logistic",
    eval_metric="aucpr",
    n_estimators=800,
    learning_rate=0.03,
    max_depth=5,
    subsample=0.8,
    colsample_bytree=0.8,
    scale_pos_weight=10,
    random_state=42,
    tree_method="hist",
    n_jobs=-1
)
```

### 튜닝 포인트

| 파라미터               |                 추천 범위 | 의미         |
| ------------------ | --------------------: | ---------- |
| `max_depth`        |               3, 5, 7 | 트리 깊이      |
| `learning_rate`    | 0.01, 0.03, 0.05, 0.1 | 학습률        |
| `n_estimators`     |            300 ~ 1500 | 트리 개수      |
| `subsample`        |             0.7 ~ 1.0 | 행 샘플링      |
| `colsample_bytree` |             0.7 ~ 1.0 | 컬럼 샘플링     |
| `scale_pos_weight` |           음성/양성 비율 기준 | 클래스 불균형 보정 |

---

## 2.4 범주형 변수가 살아있다면 추천: CatBoost

### 목적

CatBoost는 범주형 변수를 잘 처리하는 gradient boosting 모델이다. 현재 데이터에 `city_name`, `forest_type_code`, `tree_species`, `age_class_code`, `density_code` 같은 범주형 또는 코드형 변수가 남아 있다면 CatBoost를 비교 모델로 사용해볼 만하다.

장점은 다음과 같다.

* 범주형 변수를 one-hot encoding 없이 처리할 수 있다.
* 범주형 변수의 조합 효과를 잘 잡는다.
* 기본 설정만으로도 성능이 안정적인 편이다.
* feature importance를 확인할 수 있다.

### 추천 사용 상황

```text
city_name, forest_type_code, tree_species 등 범주형 변수를 활용하고 싶은 경우
LightGBM/XGBoost와 다른 방식의 boosting 모델을 비교하고 싶은 경우
임상·행정구역·지형 코드의 조합 효과를 보고 싶은 경우
```

### 추천 초기 설정

```python
from catboost import CatBoostClassifier

model = CatBoostClassifier(
    loss_function="Logloss",
    eval_metric="AUC",
    iterations=1000,
    learning_rate=0.05,
    depth=6,
    l2_leaf_reg=5,
    auto_class_weights="Balanced",
    random_seed=42,
    verbose=100
)
```

범주형 컬럼이 있다면 다음처럼 지정한다.

```python
cat_features = [
    "city_name",
    "forest_type_code",
    "forest_origin_code",
    "forest_exist_code",
    "tree_species",
    "age_class_code",
    "diameter_class_code",
    "density_code",
    "month"
]

model.fit(
    X_train,
    y_train,
    cat_features=cat_features,
    eval_set=(X_valid, y_valid),
    use_best_model=True
)
```

### 주의점

`forest_type_code`, `age_class_code`처럼 숫자로 되어 있지만 실제로는 범주형 의미를 가지는 컬럼은 CatBoost에서 categorical feature로 지정하는 것이 좋다.

단, `dwi`, `ffdri`, `pffdri`, `effective_humidity`, `elevation`, `slope`, `pei`처럼 연속적인 수치형 변수는 categorical feature로 지정하면 안 된다.

### 해석 포인트

CatBoost 결과가 LightGBM보다 좋다면 다음과 같이 해석할 수 있다.

```text
행정구역, 임상 코드, 산림유형 등 범주형 공간 정보의 조합이 산불 발생 라벨 구분에 기여했을 가능성이 있다.
```

반대로 CatBoost가 LightGBM보다 좋지 않아도 문제는 아니다.

```text
현재 데이터에서는 범주형 조합보다 DWI, 실효습도, FFDRI 같은 연속형 위험지수의 설명력이 더 크게 작용한 것으로 해석할 수 있다.
```

---

## 2.5 빠른 tree baseline: ExtraTrees

### 목적

ExtraTrees는 Random Forest와 비슷하지만, 분할 기준에 더 많은 무작위성을 부여하는 모델이다. Random Forest보다 빠르게 강한 baseline을 만들 수 있는 경우가 많다.

장점은 다음과 같다.

* 사용하기 쉽다.
* Random Forest보다 빠른 경우가 많다.
* 비선형 관계를 어느 정도 잡을 수 있다.
* feature importance를 확인할 수 있다.

### 추천 사용 상황

```text
Random Forest보다 빠른 tree baseline이 필요한 경우
boosting 모델 전에 간단한 비선형 baseline을 만들고 싶은 경우
```

### 추천 초기 설정

```python
from sklearn.ensemble import ExtraTreesClassifier

model = ExtraTreesClassifier(
    n_estimators=300,
    max_depth=None,
    min_samples_leaf=5,
    class_weight="balanced",
    random_state=42,
    n_jobs=-1
)
```

### 주의점

ExtraTrees도 전체 대용량 데이터에 바로 적용하면 시간이 오래 걸릴 수 있다. 먼저 `eda_sample_500m.parquet` 또는 샘플링된 학습 데이터에서 테스트하는 것이 좋다.

---

## 2.6 Random Forest는 baseline 정도로만 추천

### 목적

Random Forest는 직관적이고 사용하기 쉬운 모델이다. 하지만 데이터가 커질수록 느리고 메모리를 많이 먹을 수 있다.

따라서 전체 데이터에 쓰기보다는 `eda_sample_500m.parquet` 같은 샘플 데이터에서 baseline 비교용으로만 사용하는 것이 적절하다.

### 추천 사용 상황

```text
tree 기반 baseline
Logistic Regression보다 복잡한 비선형 baseline
feature importance 간단 확인
```

### 추천 초기 설정

```python
from sklearn.ensemble import RandomForestClassifier

model = RandomForestClassifier(
    n_estimators=300,
    max_depth=None,
    min_samples_leaf=5,
    class_weight="balanced_subsample",
    random_state=42,
    n_jobs=-1
)
```

### 주의점

Random Forest는 발표용 baseline으로는 좋지만, 최종 모델로 강하게 밀기는 어렵다. LightGBM, XGBoost, CatBoost보다 학습 속도와 성능 면에서 불리할 수 있다.

---

## 2.7 sklearn만으로 빠르게 시도할 수 있는 모델: HistGradientBoosting

### 목적

HistGradientBoostingClassifier는 scikit-learn에서 제공하는 histogram 기반 gradient boosting 모델이다. LightGBM이나 XGBoost 설치 문제가 있거나, sklearn만으로 빠르게 boosting baseline을 만들고 싶을 때 사용할 수 있다.

장점은 다음과 같다.

* scikit-learn 기본 모델이라 사용이 쉽다.
* histogram 기반이라 일반 GradientBoosting보다 빠르다.
* tabular 데이터에서 무난한 성능을 낼 수 있다.

### 추천 사용 상황

```text
LightGBM 설치가 어렵거나 비교 모델이 하나 더 필요한 경우
sklearn 기반 boosting baseline이 필요한 경우
```

### 추천 초기 설정

```python
from sklearn.ensemble import HistGradientBoostingClassifier
from sklearn.utils.class_weight import compute_sample_weight

sample_weight = compute_sample_weight(
    class_weight="balanced",
    y=y_train
)

model = HistGradientBoostingClassifier(
    learning_rate=0.05,
    max_iter=500,
    max_leaf_nodes=31,
    l2_regularization=0.1,
    random_state=42
)

model.fit(X_train, y_train, sample_weight=sample_weight)
```

### 주의점

LightGBM이나 XGBoost처럼 다양한 불균형 제어 옵션이 많지는 않다. 최종 모델보다는 보조 비교 모델로 적합하다.

---

## 2.8 SVM은 비추천

SVM은 이론적으로 좋은 분류 모델이지만, 현재 프로젝트에는 추천하지 않는다.

이유는 다음과 같다.

* 대용량 데이터에서 학습이 느리다.
* feature가 많고 데이터가 큰 상황에 부담이 크다.
* 확률 예측과 feature importance 해석이 tree boosting보다 불편하다.
* 산불 라벨처럼 불균형이 심한 데이터에서는 튜닝 부담이 크다.

따라서 SVM은 이번 프로젝트의 제한 시간과 데이터 규모를 고려하면 제외하는 것이 좋다.

---

## 2.9 MLP / TabNet / 딥러닝 계열은 후순위

딥러닝 계열 모델은 시도해볼 수는 있지만, 이번 프로젝트에서는 우선순위가 낮다.

이유는 다음과 같다.

* 현재 데이터는 이미지나 시계열 원본이 아니라 정형 tabular feature이다.
* 산불 발생 라벨은 희소하고 불균형하다.
* 학습 안정성과 튜닝 비용이 크다.
* 짧은 프로젝트 기간 안에 LightGBM보다 명확한 장점을 보이기 어렵다.
* 발표에서 해석하기 어렵다.

따라서 딥러닝 계열은 시간이 충분할 때 추가 실험으로만 추천한다.

### 사용할 수 있는 경우

```text
LightGBM, XGBoost, CatBoost까지 완료한 뒤 추가 비교 실험이 필요한 경우
연속형 feature가 충분히 많고, neural network baseline을 하나 추가하고 싶은 경우
```

하지만 현재 우선순위는 아니다.

---

## 2.10 단순 지수 기반 baseline도 반드시 포함

모델링을 한다고 해서 반드시 머신러닝 모델만 비교할 필요는 없다. 오히려 이 프로젝트에서는 `DWI`, `FFDRI`, `P-FFDRI` 자체를 모델처럼 사용하는 baseline이 중요하다.

### 목적

```text
공식 위험지수 또는 확장 위험지수 자체가 산불 발생 라벨을 얼마나 잘 선별하는지 확인
```

### 방법

각 지수를 예측 점수로 보고 다음 지표를 계산한다.

```text
DWI score
FFDRI score
P-FFDRI score
```

그리고 실제 `fire_label`과 비교해 다음을 계산한다.

```text
ROC-AUC
PR-AUC
Recall@Top10%
Recall@Top20%
```

### 해석

이 baseline은 발표에서 매우 중요하다.

```text
DWI 단독보다 FFDRI가 나은가?
FFDRI보다 P-FFDRI가 나은가?
머신러닝 보정 모델이 P-FFDRI보다 나은가?
```

최종 모델 비교표에는 반드시 다음 항목을 넣는 것이 좋다.

```text
DWI 단독
FFDRI 단독
P-FFDRI 단독
Logistic Regression
LightGBM
CatBoost 또는 XGBoost
```

---

## 2.11 최종 모델 우선순위

시간이 매우 부족한 경우에는 다음 세 개만 수행해도 충분하다.

| 우선순위 | 모델                          | 역할                 |
| ---: | --------------------------- | ------------------ |
|    1 | DWI / FFDRI / P-FFDRI 단독 점수 | 지수 baseline        |
|    2 | Logistic Regression         | 해석 가능한 선형 baseline |
|    3 | LightGBM                    | 최종 추천 모델           |

시간이 조금 더 있다면 다음 모델을 추가한다.

| 우선순위 | 모델            | 역할                 |
| ---: | ------------- | ------------------ |
|    4 | CatBoost      | 범주형 변수 활용 비교 모델    |
|    5 | XGBoost       | boosting 계열 비교 모델  |
|    6 | ExtraTrees    | 빠른 tree baseline   |
|    7 | Random Forest | 직관적인 tree baseline |

시간 여유가 많을 때만 추가한다.

| 우선순위 | 모델                   | 역할                        |
| ---: | -------------------- | ------------------------- |
|    8 | HistGradientBoosting | sklearn 기반 boosting 보조 모델 |
|    9 | MLP / TabNet         | 추가 실험용                    |
|   제외 | SVM                  | 대용량·해석·튜닝 부담으로 비추천        |

---

## 2.12 팀원 5명 모델링 분담 예시

팀원 5명이 모두 모델링을 한다면 같은 모델을 중복으로 돌리기보다 역할을 나누는 것이 좋다.

| 담당 | 역할                                                      | 산출물                           |
| -- | ------------------------------------------------------- | ----------------------------- |
| 1명 | DWI / FFDRI / P-FFDRI 단독 baseline + Logistic Regression | 기준 성능표                        |
| 1명 | LightGBM                                                | 주력 모델 성능 및 feature importance |
| 1명 | CatBoost                                                | 범주형 변수 활용 모델 비교               |
| 1명 | XGBoost 또는 ExtraTrees                                   | 비교 모델                         |
| 1명 | 500m / 1km / 2km 라벨 민감도 비교 및 결과 통합                      | 최종 비교표, 발표 그림                 |

이렇게 나누면 발표에서 다음 흐름을 만들 수 있다.

```text
1. 공식 지수 자체의 성능 확인
2. 선형 baseline 확인
3. tree boosting 모델로 보정
4. CatBoost/XGBoost와 비교
5. 공간 라벨 해상도별 민감도 확인
```

---

## 2.13 최종 추천 조합

최종 보고서와 발표에 가장 적합한 모델 조합은 다음과 같다.

```text
DWI 단독
FFDRI 단독
P-FFDRI 단독
Logistic Regression
LightGBM
CatBoost
XGBoost
```

시간이 부족하면 다음 조합으로 줄인다.

```text
DWI 단독
FFDRI 단독
P-FFDRI 단독
Logistic Regression
LightGBM
```

가장 중요한 것은 모델 개수를 많이 늘리는 것이 아니라, 각 모델이 어떤 의미를 가지는지 설명할 수 있어야 한다는 점이다.

```text
DWI / FFDRI / P-FFDRI: 지수 baseline
Logistic Regression: 해석 가능한 선형 baseline
LightGBM: 최종 보정 모델
CatBoost: 범주형 공간 정보 활용 모델
XGBoost: boosting 계열 안정성 비교 모델
```

---

# 3. 데이터 분할 전략

## 3.1 랜덤 분할만 사용하면 위험함

산불 데이터는 시간성과 공간성이 강하다. 단순 random split을 사용하면 같은 연도, 비슷한 지역, 비슷한 기상 조건이 train/test에 섞여 성능이 과대평가될 수 있다.

따라서 발표용으로는 최소한 연도 기준 검증을 하나 포함하는 것이 좋다.

---

## 3.2 추천 split

### 빠른 baseline

```text
train_test_split(stratify=fire_label, test_size=0.2)
```

장점:
- 빠르다.
- 모델이 돌아가는지 확인하기 좋다.

단점:
- 실제 미래 예측 성능보다 좋게 나올 수 있다.

---

### 발표용 추천 split

```text
train: 2020~2023
valid/test: 2024
```

장점:
- 미래 연도 예측에 가까운 검증이다.
- 발표에서 설득력이 높다.

단점:
- 2024년에 양성 라벨이 적으면 성능 변동이 클 수 있다.

---

### 2025년 처리

현재 2025년은 feature는 생성되어 있지만 산불 라벨은 0으로 보일 가능성이 높다. 2025년을 학습 데이터에 섞으면 안 된다.

추천 처리:

```text
2020~2024: 학습/검증용
2025: 예측 대상 기간 또는 향후 적용 시뮬레이션 기간
```

---

# 4. 하이퍼파라미터 튜닝 팁

## 4.1 튜닝보다 먼저 baseline 고정

시간이 얼마 남지 않았으므로 처음부터 Optuna나 GridSearch를 크게 돌리는 것은 추천하지 않는다.

먼저 다음 순서로 진행한다.

```text
1. 기본 파라미터로 모델 학습
2. 성능 지표 확인
3. feature importance 확인
4. 주요 파라미터 3~5개만 좁게 튜닝
```

---

## 4.2 LightGBM 추천 초기값

처음에는 다음 정도로 시작하면 된다.

```python
from lightgbm import LGBMClassifier

model = LGBMClassifier(
    objective="binary",
    n_estimators=500,
    learning_rate=0.03,
    num_leaves=31,
    max_depth=-1,
    min_child_samples=50,
    subsample=0.8,
    colsample_bytree=0.8,
    class_weight="balanced",
    random_state=42,
    n_jobs=-1
)
```

이 설정은 너무 공격적이지 않고, baseline으로 쓰기 좋다.

---

## 4.3 LightGBM 주요 튜닝 포인트

| 파라미터 | 추천 범위 | 의미 |
|---|---|---|
| `n_estimators` | 300 ~ 1000 | 트리 개수 |
| `learning_rate` | 0.01 ~ 0.05 | 학습률 |
| `num_leaves` | 15 ~ 63 | 트리 복잡도 |
| `max_depth` | 3 ~ 10 또는 -1 | 트리 깊이 제한 |
| `min_child_samples` | 30 ~ 200 | leaf 최소 샘플 수 |
| `subsample` | 0.7 ~ 1.0 | 행 샘플링 |
| `colsample_bytree` | 0.7 ~ 1.0 | 컬럼 샘플링 |
| `class_weight` | `balanced` | 클래스 불균형 보정 |

---

## 4.4 튜닝 방향

### 과적합이 의심될 때

다음처럼 복잡도를 낮춘다.

```text
num_leaves 낮추기
max_depth 낮추기
min_child_samples 높이기
subsample 낮추기
colsample_bytree 낮추기
```

### 성능이 너무 낮을 때

다음처럼 표현력을 늘린다.

```text
n_estimators 증가
num_leaves 증가
max_depth 증가
learning_rate 약간 감소
```

### 양성 recall이 너무 낮을 때

산불은 희소 사건이므로 양성 recall이 낮게 나올 수 있다. 이때는 다음을 조정한다.

```text
class_weight="balanced" 적용
scale_pos_weight 조정
threshold를 0.5보다 낮추기
top-k 기반 평가 사용
```

---

## 4.5 XGBoost 추천 초기값

XGBoost를 사용할 경우에는 다음 정도로 시작한다.

```python
from xgboost import XGBClassifier

model = XGBClassifier(
    n_estimators=500,
    learning_rate=0.03,
    max_depth=5,
    subsample=0.8,
    colsample_bytree=0.8,
    eval_metric="logloss",
    tree_method="hist",
    random_state=42,
    n_jobs=-1
)
```

데이터가 크다면 반드시 `tree_method="hist"`를 사용하는 것이 좋다.

---

## 4.6 Logistic Regression 추천 설정

```python
from sklearn.pipeline import Pipeline
from sklearn.preprocessing import StandardScaler
from sklearn.linear_model import LogisticRegression

model = Pipeline([
    ("scaler", StandardScaler()),
    ("clf", LogisticRegression(
        class_weight="balanced",
        max_iter=1000,
        random_state=42
    ))
])
```

Logistic Regression은 성능보다는 해석과 baseline 역할로 사용한다.

---

# 5. 성능 지표 추천

## 5.1 Accuracy는 추천하지 않음

산불 발생은 희소 사건이다. 비발생이 훨씬 많기 때문에, 모든 샘플을 0으로 예측해도 accuracy가 높게 나올 수 있다.

따라서 Accuracy를 핵심 지표로 쓰면 안 된다.

---

## 5.2 추천 지표

| 지표 | 추천도 | 이유 |
|---|---:|---|
| ROC-AUC | 높음 | 전체적인 분류 성능 비교에 좋음 |
| PR-AUC | 매우 높음 | 희소 양성 데이터에 적합 |
| Recall | 높음 | 실제 산불 발생 격자를 얼마나 놓치지 않는지 확인 |
| Precision | 보조 | 경보 남발 여부 확인 |
| F1-score | 보조 | Precision/Recall 균형 |
| Recall@Top10% | 매우 높음 | 고위험 격자 선별 목적에 적합 |
| Recall@Top20% | 매우 높음 | 실무적 관리 범위 평가에 적합 |
| Lift@Top10% | 높음 | 랜덤 대비 고위험 선별 효과 확인 |

---

## 5.3 가장 추천하는 발표용 지표

이번 프로젝트의 목적은 단순히 산불 발생 여부를 맞히는 것보다, 고위험 격자를 선별하는 데 가깝다.

따라서 발표용 핵심 지표는 다음이 가장 좋다.

```text
Recall@Top10%
Recall@Top20%
PR-AUC
ROC-AUC
```

특히 `Recall@Top10%`와 `Recall@Top20%`는 기존 EDA 결과와 연결하기 좋다.

이미 500m EDA에서 다음 결과가 나왔다.

| 지수 | Top10% 양성 포착률 | Top20% 양성 포착률 |
|---|---:|---:|
| P-FFDRI | 20.81% | 36.67% |
| FFDRI | 20.59% | 35.04% |
| DWI | 17.85% | 29.70% |

모델링 결과도 같은 방식으로 비교하면 좋다.

예시:

| 모델/지수 | ROC-AUC | PR-AUC | Recall@Top10% | Recall@Top20% |
|---|---:|---:|---:|---:|
| DWI 단독 | - | - | 17.85% | 29.70% |
| FFDRI | - | - | 20.59% | 35.04% |
| P-FFDRI | - | - | 20.81% | 36.67% |
| Logistic Regression | 0.xx | 0.xx | xx% | xx% |
| LightGBM | 0.xx | 0.xx | xx% | xx% |

이렇게 만들면 발표 스토리가 매우 깔끔해진다.

---

## 5.4 Recall@TopK 계산 의미

`Recall@Top10%`는 다음 의미다.

```text
모든 격자-일 샘플을 모델 예측 위험도 순으로 정렬한다.
그중 상위 10%만 고위험으로 지정한다.
이 상위 10% 안에 실제 산불 발생 샘플이 전체 양성 중 몇 % 포함되었는지 계산한다.
```

즉 실무적으로는 다음 질문에 답한다.

```text
전체 지역을 다 관리할 수 없을 때,
모델이 지정한 상위 10% 고위험 격자만 집중 관리하면
실제 산불 발생 격자의 몇 %를 잡을 수 있는가?
```

이 프로젝트의 목적과 잘 맞는다.

---

# 6. 모델링 실험 설계 추천

## 6.1 최소 실험 세트

시간이 부족하면 아래 실험만 수행해도 된다.

| 실험 | 입력 변수 | 목적 |
|---|---|---|
| DWI 단독 | `dwi` | 기상위험 baseline |
| FFDRI 단독 | `ffdri` | 공식형 위험지수 baseline |
| P-FFDRI 단독 | `pffdri` | 확장 위험지수 baseline |
| Logistic Regression | 주요 피처 전체 | 선형 baseline |
| LightGBM | 주요 피처 전체 | 최종 보정 모델 |

이 실험 구조는 발표에서 매우 설명하기 좋다.

---

## 6.2 권장 ablation 실험

시간이 조금 더 있다면 다음 ablation을 추천한다.

| 실험명 | 사용 피처 | 목적 |
|---|---|---|
| Weather only | 기상 + 실효습도 + RNE | 기상 요인만의 성능 |
| Official index | DWI + FMI + TMI + FFDRI | 공식 산불위험지수 구조 검증 |
| Extended index | Official + YWI + PEI + P-FFDRI | 확장 변수 효과 검증 |
| Full features | 전체 주요 변수 | 최종 모델 성능 |

발표 문장:

```text
단일 지수와 피처 그룹별 모델을 비교하여, 기상위험지수에 임상·지형·전력설비·양간지풍 변수를 추가했을 때 고위험 격자 선별 성능이 개선되는지 확인하였다.
```

---

## 6.3 500m / 1km / 2km 라벨 민감도

현재 target은 500m, 1km, 2km 기준으로 생성되어 있다. 이건 중요한 장점이다.

비교 목적:

| 라벨 기준 | 해석 |
|---|---|
| 500m | 정밀한 발생 위치 기준, 라벨이 엄격함 |
| 1km | 위치 오차와 산불 영향권을 어느 정도 허용 |
| 2km | 더 넓은 영향권 반영, 단 라벨 노이즈 증가 가능 |

추천 실험:

```text
같은 LightGBM 설정으로 500m, 1km, 2km 각각 학습
ROC-AUC, PR-AUC, Recall@Top10%, Recall@Top20% 비교
```

발표 문장:

```text
산불 발생 위치의 좌표 오차와 공간 영향권을 고려하기 위해 500m, 1km, 2km 라벨을 각각 생성하고, 라벨 반경에 따른 모델 성능 변화를 비교하였다.
```

---

# 7. 모델링 시 주의할 점

## 7.1 EDA 샘플은 실제 발생률을 의미하지 않음

`eda_sample`은 양성 전체와 음성 샘플을 결합한 분석용 데이터다. 따라서 이 데이터에서의 positive rate는 실제 산불 발생률이 아니다.

발표에서는 다음처럼 말해야 한다.

```text
전체 비발생 격자가 매우 많기 때문에, EDA 및 1차 모델링에서는 산불 발생 격자를 전체 포함하고 비발생 격자를 샘플링하여 사용하였다.
```

---

## 7.2 Threshold 0.5에 집착하지 않기

산불 예측에서는 확률 0.5를 넘는지만 보는 방식이 적절하지 않을 수 있다. 실제 목적은 고위험 격자를 우선순위화하는 것이다.

따라서 다음 방식이 더 적절하다.

```text
예측 확률이 높은 순으로 정렬
상위 10%, 20% 격자를 고위험으로 지정
그 안에 실제 산불 발생이 얼마나 포함되는지 평가
```

---

## 7.3 TMI는 한계로 명시

현재 TMI 계열 변수는 공식 TMI 완전 재현이라기보다 proxy 성격이 있다.

따라서 모델에는 넣어도 되지만, 발표에서는 다음처럼 말하는 것이 안전하다.

```text
현재 TMI 계열 변수는 지형 proxy로 구현되어 있으며, 향후 공식 고도·방위 등급 체계를 반영한 보정이 필요하다.
```

---

## 7.4 PEI는 핵심 예측변수보다 관리 우선순위 변수로 해석

500m EDA에서 PEI는 산불 발생 격자에서 소폭 높았지만, DWI나 FFDRI처럼 강한 구분력을 보이지는 않았다.

따라서 다음처럼 해석하는 것이 적절하다.

```text
PEI는 산불 발생 자체를 강하게 예측하는 단독 변수라기보다는, 전력설비 점검 우선순위와 고위험 격자 관리 관점에서 의미 있는 보조 지표로 해석하였다.
```

---

# 8. 팀원 5명 모델링 역할 분담안

5명이 모델링을 한다면 서로 같은 일을 하기보다는 역할을 나누는 것이 좋다.

| 담당 | 역할 | 산출물 |
|---|---|---|
| 1명 | 데이터 로딩/피처 정리 | 모델링용 train/test parquet 또는 csv |
| 1명 | Logistic / RandomForest baseline | baseline 성능표 |
| 1명 | LightGBM 최종 모델 | 주요 성능 결과 |
| 1명 | 500m / 1km / 2km 라벨 비교 | 라벨 민감도 표 |
| 1명 | feature importance / SHAP / 발표 정리 | 변수 중요도 그림, 해석 문장 |

현실적으로는 LightGBM 담당과 발표 정리 담당이 가장 중요하다.

---

# 9. 발표에 넣기 좋은 최종 결과표 형태

## 9.1 모델 성능표

| 모델 | 입력 변수 | ROC-AUC | PR-AUC | Recall@Top10% | Recall@Top20% |
|---|---|---:|---:|---:|---:|
| DWI 단독 | `dwi` | - | - | 17.85% | 29.70% |
| FFDRI 단독 | `ffdri` | - | - | 20.59% | 35.04% |
| P-FFDRI 단독 | `pffdri` | - | - | 20.81% | 36.67% |
| Logistic Regression | 주요 피처 | 0.xx | 0.xx | xx% | xx% |
| LightGBM | 전체 주요 피처 | 0.xx | 0.xx | xx% | xx% |

---

## 9.2 피처 중요도표

| 순위 | 변수 | 해석 |
|---:|---|---|
| 1 | `effective_humidity` | 누적 건조도 |
| 2 | `hm_min` | 최저습도 |
| 3 | `dwi` | 기상위험지수 |
| 4 | `pffdri` | 확장 위험지수 |
| 5 | `rne` | 강수 감쇠 효과 |
| 6 | `pei` | 전력설비·접근성 보조 지표 |

실제 순위는 모델 결과에 따라 바뀔 수 있다. 위 표는 예상되는 해석 틀이다.

---

# 10. 최종 권장 전략

시간이 얼마 남지 않았으므로 다음 순서로 진행하는 것이 가장 안전하다.

## Step 1. 500m 샘플로 빠른 baseline

```text
output/eda/eda_sample_500m.parquet
```

사용 모델:

```text
Logistic Regression
LightGBM
```

평가:

```text
ROC-AUC
PR-AUC
Recall@Top10%
Recall@Top20%
```

---

## Step 2. 단일 지수와 모델 비교

비교 대상:

```text
DWI
FFDRI
P-FFDRI
Logistic Regression
LightGBM
```

목적:

```text
공식 위험지수 대비 ML 보정 모델이 고위험 격자 선별 성능을 얼마나 개선하는지 확인
```

---

## Step 3. 1km / 2km 라벨도 같은 방식으로 비교

시간이 있으면 같은 실험을 `eda_sample_1km.parquet`, `eda_sample_2km.parquet`에도 적용한다.

목적:

```text
산불 발생 위치의 공간 오차와 영향권을 고려했을 때 모델 성능이 어떻게 바뀌는지 확인
```

---

## Step 4. feature importance 확인

LightGBM feature importance를 확인하여 다음 질문에 답한다.

```text
모델이 실제로 어떤 변수를 중요하게 봤는가?
DWI / FFDRI / P-FFDRI가 중요한가?
실효습도와 습도 계열이 중요한가?
PEI와 YWI는 보조적으로 의미가 있는가?
```

---

# 11. 발표용 핵심 문장

모델링 파트에서 사용할 수 있는 문장은 다음과 같다.

```text
전처리 단계에서 생성한 격자-일 단위 최종 피처셋과 실제 산불 발생 이력 기반 target 라벨을 결합하여 산불 발생 여부 예측 모델을 구축하였다. 산불은 희소 사건이므로 단순 정확도보다는 PR-AUC와 상위 위험 격자 포착률을 중심으로 평가하였다.
```

```text
기존 산불위험지수인 DWI, FFDRI, 확장 지수인 P-FFDRI를 단일 baseline으로 두고, Logistic Regression과 LightGBM을 이용해 보정 모델을 학습하였다. 이를 통해 공식 위험지수 기반 접근과 머신러닝 기반 보정 접근의 성능을 비교하였다.
```

```text
실무적으로는 모든 격자를 동일하게 관리하기 어렵기 때문에, 예측 확률 상위 10%와 20% 격자에 실제 산불 발생 격자가 얼마나 포함되는지를 주요 지표로 사용하였다.
```

---

# 12. 결론

현재 산출물은 모델링을 시작하기에 충분하다.

모델링에 사용할 핵심 데이터는 다음이다.

```text
1. output/eda/eda_sample_500m.parquet
2. output/eda/eda_sample_1km.parquet
3. output/eda/eda_sample_2km.parquet
4. output/final/final_feature_daily/ + output/target/target_fire_daily_*.parquet
```

가장 추천하는 모델은 다음이다.

```text
1. Logistic Regression: baseline 및 해석용
2. LightGBM: 최종 추천 모델
```

가장 추천하는 평가지표는 다음이다.

```text
1. PR-AUC
2. ROC-AUC
3. Recall@Top10%
4. Recall@Top20%
```

가장 중요한 발표 메시지는 다음이다.

```text
공식 산불위험지수와 확장 P-FFDRI는 실제 산불 발생 라벨과 정합적인 방향성을 보였으며, 머신러닝 모델은 이를 기반으로 고위험 격자 선별 성능을 보정·개선하는 역할을 한다.
```
