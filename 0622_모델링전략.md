# 0622 모델링 전략

## 0. 문서 목적

본 문서는 `최종공모안_V1.2.pdf`의 **[분석 방법] 및 [결과 및 검증]** 파트를 정리하기 위한 모델링 전략 문서이다.

현재 팀원별 모델링 결과는 다음과 같은 문제가 있다.

- 모델별 학습 데이터 샘플링 기준이 통일되어 있지 않다.
- Logistic Regression, Random Forest, LightGBM, XGBoost의 평가 조건이 서로 다르다.
- 샘플 평가와 2024년 전체 모집단 평가가 혼재되어 있다.
- threshold 기반 평가와 Top-K 평가가 함께 쓰이지만, 두 평가의 목적 차이가 명확히 설명되지 않았다.
- 최종 제출 대상인 `test_hanjeon.csv`는 전신주별 `decision` 0/1을 요구하지만, 일별 위험도를 전신주 단위로 집계하는 기준이 아직 명확하지 않다.

따라서 본 문서는 최종 제출용 보고서에 사용할 수 있도록 **모델 선정 이유, 샘플링 기준, 평가 지표 선정 이유, 최종 decision 산출 기준**을 하나의 일관된 전략으로 정리한다.

---

## 1. 문제 정의

본 과제는 산불 발생 여부를 정확하게 맞히는 일반적인 이진분류 문제가 아니라, **전력설비 주변의 고위험 후보군을 사전에 선별하는 ranking 문제**로 정의하는 것이 적절하다.

산불 위험도가 높더라도 실제 산불은 항상 발생하지 않는다. 산불은 기상 조건, 연료 조건, 지형 조건, 발화원, 사람의 활동, 우연성이 함께 작용할 때 발생한다. 따라서 고위험 조건이 존재했지만 실제 산불이 발생하지 않은 격자를 단순한 오탐으로만 해석하기 어렵다.

이에 본 분석의 모델링 목표는 다음과 같이 정의한다.

```text
목표: 산불 발생을 확정적으로 예측하는 것이 아니라,
      제한된 점검·순찰 자원 하에서 우선 관리가 필요한
      전력설비 주변 고위험 후보군을 선별한다.
```

이 정의에 따라 모델 성능은 Accuracy 중심이 아니라, **실제 산불 발생 격자가 고위험 후보군 안에 얼마나 포함되는지**를 중심으로 평가한다.

---

## 2. 2025 최우수상 보고서에서 가져올 모델링 관점

2025년 최우수상 보고서들의 공통점은 단순히 모델을 많이 비교한 것이 아니라, **데이터 특성 → 문제 진단 → 모델 선택 → 평가 기준**이 하나의 논리로 연결된다는 점이다.

### 2.1 열수요 예측 보고서에서 배울 점

열수요 예측 보고서는 초과 생산과 부족 생산이 비용 문제로 이어진다는 점을 먼저 정의한 뒤, 열수요 데이터의 계절별 스케일 불균형과 무수요 구간을 고려하여 Tweedie 손실함수를 선택하였다.

우리 프로젝트에 적용하면 다음과 같다.

```text
산불은 극단적으로 희소한 사건이다.
위험도가 높아도 실제 발화가 일어나지 않는 경우가 많다.
따라서 모델은 발생 확률을 정확히 맞히는 것보다,
상대적으로 위험이 높은 후보군을 잘 정렬하는 것이 중요하다.
```

### 2.2 119 신고 예측 보고서에서 배울 점

119 신고 예측 보고서는 예측 결과를 자원 배치, 리스크 맵, 현장 운영 전략으로 연결하였다.

우리 프로젝트도 모델 결과를 다음 운영 문제와 연결해야 한다.

```text
산불 위험도 예측 → 고위험 격자 선별 → 전신주 위치 역매핑 → 점검·순찰 우선순위 산정
```

### 2.3 지하철 혼잡도 예측 보고서에서 배울 점

지하철 혼잡도 보고서는 기상 변수만으로는 혼잡도를 충분히 설명하기 어렵다는 점을 진단한 뒤, 구조적 요인을 제어하고 기상 변수의 추가 변동성을 학습하는 전략을 취하였다.

우리 프로젝트에서도 단일 기상 변수만으로 산불 발생을 설명하기 어렵기 때문에, 다음 흐름이 필요하다.

```text
단일 위험지수 baseline
→ 선형 baseline
→ 비선형 보정 모델
→ 전신주 단위 운영 decision
```

---

## 3. 모델링 전체 전략

최종 보고서에서는 모델링을 다음 4단계로 정리한다.

```text
1단계. 단일 위험지수 baseline
2단계. 선형 baseline
3단계. 비선형 보정 모델
4단계. 전신주 단위 decision 산출
```

### 3.1 1단계: 단일 위험지수 baseline

사용 모델 또는 점수는 다음과 같다.

| 구분 | 내용 | 선정 이유 |
|---|---|---|
| DWI | 기상위험지수 | 건조·습도·풍속 등 직접 기상위험을 반영하는 기준선 |
| FFDRI | 기존 산불위험지수 | 산림청 산불위험지수 구조를 참고한 공식형 기준선 |
| P-FFDRI | 전력설비 확장 위험지수 | 기존 위험지수에 전력설비 노출도, 지형, 양간지풍 위험을 결합한 프로젝트 핵심 지수 |

단일 위험지수 baseline은 반드시 포함해야 한다. 이유는 다음과 같다.

- 산불위험지수 자체가 이미 도메인 지식을 반영한 강한 기준선이다.
- ML 모델이 실제로 위험지수보다 개선되는지 확인할 수 있다.
- ML 모델이 기대만큼 좋지 않더라도 P-FFDRI의 타당성을 보고서의 핵심 결과로 제시할 수 있다.

### 3.2 2단계: 선형 baseline

선형 baseline으로 Logistic Regression을 사용한다.

| 모델 | 입력 피처 | 선정 이유 |
|---|---|---|
| Logistic Regression Index-only | `dwi`, `ffdri`, `pffdri` | 위험지수 조합만으로 산불 라벨을 어느 정도 설명할 수 있는지 확인 |
| Logistic Regression Compact | 기상 핵심 + 위험지수 일부 | 선형 모델에서 기상·위험지수 조합의 기준 성능 확인 |

Logistic Regression을 사용하는 이유는 다음과 같다.

- 학습 속도가 빠르므로 더 큰 negative sample을 사용할 수 있다.
- StandardScaler 적용 후 계수 방향을 참고할 수 있어 설명 가능성이 높다.
- 최종 고성능 모델이라기보다, 비선형 모델로 넘어가기 전 기준선 역할을 한다.
- 전체 feature를 모두 넣었을 때와 index-only를 비교하면 중복 파생변수와 다중공선성 문제를 진단할 수 있다.

### 3.3 3단계: 비선형 보정 모델

최종 후보 모델은 tree-based ensemble을 중심으로 비교한다.

| 모델 | 선정 이유 | 주의점 |
|---|---|---|
| LightGBM | 대용량 tabular 데이터에 강하고 학습 속도가 빠르며 비선형 상호작용을 반영 가능 | threshold 설정 시 FP 폭증 주의 |
| XGBoost | 비선형 상호작용과 임계값 효과를 잘 포착하고 성능 비교용으로 적합 | 학습 시간이 LightGBM보다 길 수 있음 |
| Random Forest | 해석이 비교적 쉽고 class imbalance에서 high-recall 후보군 선별에 활용 가능 | 대용량 데이터에서 학습 시간이 길고 FP가 많아질 수 있음 |

최종 보고서에서는 모델을 많이 나열하기보다 다음 논리로 설명한다.

```text
Logistic Regression은 선형 baseline으로 사용하고,
LightGBM, XGBoost, Random Forest는 변수 간 비선형 관계와 상호작용을 반영하기 위한 후보 모델로 비교하였다.
산불 위험은 습도, 풍속, 강수, 지형, 전력설비 노출도가 복합적으로 작용하므로 tree-based ensemble 모델이 적합하다.
```

### 3.4 4단계: 전신주 단위 decision 산출

최종 제출용 `test_hanjeon.csv`는 전신주별 `decision` 0/1을 요구한다. 그러나 위험도는 날짜별로 달라지므로, 일별 격자 위험도를 전신주 위치에 역매핑한 뒤 전신주별 기간 위험도로 집계해야 한다.

최종 decision 산출 흐름은 다음과 같다.

```text
일별 격자 위험도 산출
→ 전신주 위치를 100m grid_id에 매핑
→ 전신주별 일별 위험도 추출
→ 전신주별 기간 위험도 집계
→ 상위 위험 전신주 decision=1
```

---

## 4. 샘플링 전략

### 4.1 모델별 샘플 수를 다르게 해도 되는가?

모델 특성에 따라 학습 샘플 수를 다르게 사용하는 것은 가능하다.

예를 들어 Logistic Regression은 대용량 데이터를 빠르게 학습할 수 있지만, Random Forest나 XGBoost는 같은 샘플 수에서도 학습 시간이 크게 늘어날 수 있다. 따라서 최종 후보 모델 재학습 단계에서는 모델별로 샘플 수를 다르게 설정할 수 있다.

다만, **모델 간 성능 비교표를 만들 때는 동일한 샘플링 조건을 사용해야 한다.** 그렇지 않으면 어떤 모델이 더 좋은지 공정하게 비교하기 어렵다.

따라서 샘플링은 다음 2단계로 구분한다.

### 4.2 공통 Benchmark 샘플링

모든 모델을 같은 조건에서 비교하기 위한 공통 샘플링이다.

권장 기준은 다음과 같다.

```text
positive: 해당 기간 산불 발생 격자 전체
negative: positive 수의 20~50배 샘플링
분할: Train 2020~2022 / Validation 2023 / Test 2024
```

공통 Benchmark에서는 다음 모델을 비교한다.

```text
DWI
FFDRI
P-FFDRI
Logistic Regression
LightGBM
XGBoost
Random Forest 선택
```

보고서에는 다음과 같이 설명한다.

```text
모델 간 공정 비교를 위해 공통 benchmark 실험에서는 모든 ML 모델에 동일한 positive/negative 샘플링 조건을 적용하였다.
```

### 4.3 최종 후보 모델 재학습 샘플링

공통 Benchmark에서 성능이 좋은 모델 1~2개를 선택한 뒤, 모델 특성에 따라 학습 샘플을 확장한다.

| 모델 | 권장 negative 비율 | 이유 |
|---|---:|---|
| Logistic Regression | 50~100배 | 학습이 빠르고 큰 표본을 처리 가능 |
| LightGBM | 20~50배 | 대용량 tabular에 강하나 전체 데이터 학습은 비용 큼 |
| XGBoost | 20~50배 | 성능 비교용으로 좋지만 학습 시간이 다소 큼 |
| Random Forest | 10~20배 | 대용량에서 학습 비용이 크므로 제한 필요 |

보고서에는 다음 문장을 사용한다.

```text
동일한 샘플 조건에서 baseline 비교를 수행한 뒤, 최종 후보 모델에 대해서만 연산 비용과 모델 특성을 고려하여 학습 샘플을 확장하였다.
```

---

## 5. 데이터 분할 전략

무작위 분할은 사용하지 않는다.

산불 위험도는 계절성, 연도별 기상 조건, 공간적 자기상관의 영향을 받기 때문에, 무작위 분할을 사용하면 미래 예측 상황을 제대로 반영하지 못할 수 있다.

권장 분할은 다음과 같다.

| 구분 | 기간 | 역할 |
|---|---|---|
| Train | 2020~2022 | 모델 학습 |
| Validation | 2023 | threshold 선택, 하이퍼파라미터 튜닝 |
| Test | 2024 | 최종 평가 |

보고서에는 다음처럼 쓴다.

```text
실제 운영에서는 과거 기상·공간정보를 바탕으로 미래 시점의 산불 위험을 예측해야 하므로, 무작위 분할 대신 시간 기준 분할을 적용하였다. 2020~2022년은 학습, 2023년은 threshold 및 하이퍼파라미터 조정, 2024년은 최종 평가에 사용하였다.
```

---

## 6. Target 반경 전략

산불 발생 좌표는 실제 발화 지점을 완벽하게 나타내지 않을 수 있고, 산불은 단일 100m 격자에만 영향을 주는 사건이 아니다. 따라서 500m, 1km, 2km 라벨을 모두 생성하여 민감도를 비교한다.

| Target 반경 | 해석 | 장점 | 단점 |
|---|---|---|---|
| 500m | 엄격한 위치 기준 | 위치 정밀도 높음 | 좌표 오차에 취약, positive 수 적음 |
| 1km | 위치 오차와 주변 영향권을 고려한 타협 기준 | 실무적 해석 용이 | 일부 주변 격자 포함 |
| 2km | 넓은 영향권 기반 후보군 선별 | positive 수 증가, 영향권 반영 | 라벨 노이즈 증가 가능 |

권장 전략은 다음과 같다.

```text
기본 모델링은 1km target을 중심으로 수행하고,
500m와 2km는 라벨 반경 민감도 분석으로 함께 제시한다.
```

다만 기존 결과에서 2km 모델 성능을 활용해야 한다면 다음처럼 표현한다.

```text
2km target은 발화점 정밀 예측이 아니라 산불 영향권 기반 고위험 후보군 선별 문제로 해석한다.
```

---

## 7. Feature set 전략

전체 feature를 모두 넣는 방식은 해석이 어렵고 중복 파생변수로 인해 모델이 불안정해질 수 있다. 따라서 feature set을 나누어 비교한다.

### 7.1 추천 feature set

| 실험명 | Feature 구성 | 목적 |
|---|---|---|
| Index-only | `dwi`, `ffdri`, `pffdri` | 위험지수 자체의 설명력 확인 |
| Weather-only | 기상, 습도, 강수, DWI 계열 | 산불 발생의 직접 기상 조건 확인 |
| Compact-full | 기상 + 산림 + 지형 + 전력설비 + 종합지수 | 최종 후보 모델용 압축 feature |
| No-index | 원천·파생 변수만 사용하고 `ffdri`, `pffdri` 제외 | 위험지수 추가 효과 비교 |

### 7.2 Compact-full 추천 변수

```text
기상:
ta_mean, ta_max, hm_mean, hm_min,
wind_ws_mean, wind_ws_max,
rn_day_mean, rn_day_max,
effective_humidity, rne, dwi, dwi_n, day_weight

산림/지형:
fmi_n, tmi_p, ywi, Da, Ws, Dr,
slope, elevation

전력설비/접근성:
pei, pole_n, road_prox, river_far

종합지수:
ffdri, pffdri

시간:
month_num, day_num 또는 dayofyear
```

### 7.3 제외해야 할 변수

다음 변수는 학습 feature에서 제외한다.

```text
fire_label
fire_count
fire_objt_ids
fire_addresses
fire_amount_sum
event_lon_mean
event_lat_mean
event_x5179_mean
event_y5179_mean
target_grid_id
target_grid_size_m
target_grid_x
target_grid_y
grid_id
date
```

산불 발생 이력에서 사후에만 알 수 있는 정보는 모두 feature에서 제외해야 한다.

---

## 8. 성능 지표 선정 이유

### 8.1 Accuracy를 사용하지 않는 이유

산불 발생은 극단적으로 희소한 사건이다. 전체 격자-일 조합 중 산불이 발생하지 않는 경우가 대부분이므로, 모든 격자를 비위험으로 예측해도 Accuracy는 매우 높게 나올 수 있다.

따라서 Accuracy는 본 과제의 핵심 지표로 적절하지 않다.

### 8.2 Precision을 주 지표로 사용하지 않는 이유

고위험 조건이 존재하더라도 실제 발화원이 없으면 산불은 발생하지 않을 수 있다. 즉, 모델이 위험하다고 판단한 격자에서 산불이 발생하지 않았다고 해서 해당 격자가 운영상 의미 없는 오탐이라고 단정하기 어렵다.

따라서 Precision은 후보군 규모를 확인하는 보조 지표로 사용하고, 주 지표는 고위험 후보군 내 실제 산불 포착 성능을 중심으로 설정한다.

### 8.3 주 지표

| 지표 | 선정 이유 |
|---|---|
| 일자별 Recall@Top-K | 매일 상위 K개 고위험 후보를 점검했을 때 실제 산불 발생 격자가 포함되는지 평가 |
| Lift@Top-K | 무작위 점검 대비 모델 기반 후보군 선별이 얼마나 효율적인지 평가 |
| PR-AUC | 불균형 데이터에서 양성 class ranking 성능 확인 |
| ROC-AUC | 전체 threshold 구간에서의 ranking 성능 보조 확인 |
| F2-score | threshold 기반 후보군 선별에서 Recall에 더 큰 가중치를 부여 |
| Recall | 실제 산불 발생 격자를 놓치지 않는 정도 확인 |

### 8.4 최종 보고서 권장 지표 구성

최종 보고서에는 다음 지표를 중심으로 제시한다.

```text
주 지표:
- 일자별 Recall@Top-K
- Lift@Top-K
- PR-AUC

보조 지표:
- ROC-AUC
- F2-score
- Precision
- threshold 기반 Recall
```

보고서 문장 예시는 다음과 같다.

```text
본 과제는 산불 발생 여부를 확정적으로 예측하는 문제가 아니라, 제한된 점검 자원으로 고위험 후보군을 선별하는 문제이다. 따라서 Accuracy보다 일자별 Recall@Top-K와 Lift@Top-K를 주요 지표로 사용하고, PR-AUC와 ROC-AUC를 보조적인 ranking 성능 지표로 활용하였다.
```

---

## 9. Threshold와 Top-K 평가의 역할 구분

Threshold 기반 평가는 모델이 위험 후보군을 넓게 선별할 때 실제 산불 격자를 얼마나 놓치지 않는지 확인하는 평가다.

반면 Top-K 평가는 제한된 점검 자원 하에서 매일 상위 K개 후보만 선택했을 때 실제 산불 발생 격자가 포함되는지 확인하는 운영형 평가다.

| 평가 방식 | 목적 | 장점 | 한계 |
|---|---|---|---|
| Threshold 평가 | 위험 후보군을 넓게 선별 | Recall 해석 용이 | FP가 매우 커질 수 있음 |
| Top-K 평가 | 제한된 점검 자원 시나리오 반영 | 운영 활용성과 직접 연결 | K 선택 기준 필요 |

보고서에는 두 평가를 다음처럼 함께 설명한다.

```text
Threshold 기반 평가는 고위험 후보군을 넓게 선별하는 능력을 확인하기 위한 지표이며, Top-K 평가는 제한된 점검 자원으로 매일 상위 후보만 선택했을 때의 운영 효율을 확인하기 위한 지표이다. 따라서 두 평가는 서로 대체 관계가 아니라, 후보군 선별 폭에 따른 성능을 함께 보여주는 보완적 평가로 해석하였다.
```

---

## 10. 최종 decision 산출 전략

### 10.1 제출 파일 구조

최종 제출 대상은 `test_hanjeon.csv`이며, 기본 구조는 다음과 같다.

```text
pole_id, lon, lat, decision
```

각 행은 하나의 전신주 위치를 의미하며, `decision`에 산불 위험 여부를 0 또는 1로 입력해야 한다.

### 10.2 왜 기간 집계가 필요한가?

한 전신주는 하나의 위치에 존재하지만, 날씨와 산불 위험도는 날짜마다 변한다. 따라서 전신주별로 여러 날짜의 위험도가 존재한다.

단순히 특정 하루의 위험도만 사용하면 전체 기간의 위험 노출을 반영하기 어렵고, 단순 평균만 사용하면 짧지만 매우 위험했던 날의 신호가 희석될 수 있다.

따라서 다음 3가지를 함께 반영한다.

| 집계 요소 | 의미 |
|---|---|
| 최대 위험도 | 한 번이라도 매우 위험한 날이 있었는지 |
| 상위 위험일 평균 | 위험이 일시적 우연인지 반복되는지 |
| 고위험일 비율 | 전체 기간 중 고위험 노출 빈도 |

### 10.3 일별 위험도 산출

권장 방식은 P-FFDRI와 ML 위험확률을 함께 사용하는 것이다.

```text
daily_risk = 0.7 × P-FFDRI_percentile + 0.3 × ML_probability_percentile
```

단, ML 모델이 P-FFDRI보다 안정적이지 않거나 최종 모델 정리가 어려운 경우에는 다음처럼 보수적으로 설정할 수 있다.

```text
daily_risk = P-FFDRI_percentile
```

여기서 percentile을 사용하는 이유는 날짜마다 전체 위험도 수준이 달라지기 때문이다. 같은 P-FFDRI 값이라도 특정 날짜에는 상위 위험에 해당할 수 있고, 다른 날짜에는 평균 수준일 수 있다. 따라서 일자별 percentile rank를 사용하면 매일 상대적으로 위험한 위치를 안정적으로 선별할 수 있다.

### 10.4 전신주별 기간 위험도 집계

전신주별 최종 위험점수는 다음과 같이 정의한다.

```text
pole_risk_score =
0.5 × max(daily_risk)
+ 0.3 × mean_top5(daily_risk)
+ 0.2 × high_risk_day_ratio
```

각 항목의 의미는 다음과 같다.

| 항목 | 정의 | 의미 |
|---|---|---|
| `max(daily_risk)` | 분석 기간 중 해당 전신주의 최대 일별 위험도 | 극단 고위험 노출 |
| `mean_top5(daily_risk)` | 위험도 상위 5일 평균 | 반복적 고위험 노출 |
| `high_risk_day_ratio` | 일별 상위 5% 위험도에 포함된 날짜 비율 | 고위험 노출 빈도 |

### 10.5 decision 0/1 기준

최종 decision은 전신주별 `pole_risk_score` 기준 상위 비율로 결정한다.

권장 기준은 다음과 같다.

```text
decision = 1 if pole_risk_score 상위 5%
decision = 0 otherwise
```

상위 5%를 사용하는 이유는 다음과 같다.

- 산불 위험 후보군 선별 목적에서는 너무 엄격한 기준보다 일정 수준 넓은 후보군이 필요하다.
- 전신주 수가 약 138만 개 수준이므로 상위 5%는 약 7만 개 내외의 우선점검 후보군을 의미한다.
- 모델의 Precision이 낮은 상황에서도 Recall 관점의 후보군 선별 목적과 잘 맞는다.

운영 자원이 더 제한적이라는 설정이 필요하다면 상위 1~3% 기준도 보조 시나리오로 제시할 수 있다.

보고서 문장은 다음처럼 정리한다.

```text
최종 decision은 전신주별 기간 집계 위험점수의 상위 5%를 위험(1)으로 정의하였다. 이는 산불 발생을 확정적으로 예측하기 위한 기준이 아니라, 제한된 점검 자원 하에서 우선 점검이 필요한 전신주 후보군을 선별하기 위한 운영형 기준이다.
```

---

## 11. 최종 보고서용 모델링 구조

최종공모안의 [분석 방법] 파트는 다음 구조로 재작성하는 것이 좋다.

```text
3. 분석 방법
  가. 모델링 문제 정의
     - 산불 발생 확정 예측이 아니라 고위험 후보군 ranking 문제

  나. 모델 구성
     - 단일 위험지수 baseline: DWI, FFDRI, P-FFDRI
     - 선형 baseline: Logistic Regression
     - 비선형 보정 모델: LightGBM, XGBoost, Random Forest

  다. 데이터 분할 및 샘플링
     - Train 2020~2022
     - Validation 2023
     - Test 2024
     - 공통 benchmark는 positive 전체 + negative N배 샘플링
     - 최종 후보 모델은 연산 비용에 따라 샘플 확장

  라. 평가 지표
     - Accuracy 제외
     - 일자별 Recall@Top-K, Lift@Top-K, PR-AUC 중심
     - ROC-AUC, F2-score, Precision 보조

  마. 전신주 decision 산출
     - 일별 위험도 전신주 역매핑
     - max, top5 평균, 고위험일 비율로 pole_risk_score 생성
     - 상위 5% decision=1
```

---

## 12. 최종 보고서에 바로 쓸 수 있는 문장

### 12.1 모델 선정 이유

```text
본 과제는 산불 발생 여부를 확정적으로 예측하기보다, 제한된 점검 자원 하에서 전력설비 주변 고위험 후보군을 선별하는 것을 목표로 한다. 이에 따라 먼저 DWI, FFDRI, P-FFDRI를 단일 위험지수 baseline으로 설정하여 도메인 기반 위험지수의 설명력을 확인하였다. 이후 Logistic Regression을 선형 baseline으로 사용하여 위험지수 조합의 기준 성능을 확인하고, LightGBM, XGBoost, Random Forest와 같은 tree-based ensemble 모델을 통해 기상, 산림, 지형, 전력설비 변수 간 비선형 상호작용을 추가로 학습하였다.
```

### 12.2 샘플링 전략

```text
산불 발생 라벨은 전체 격자-일 데이터에서 극히 희소하므로, 학습 단계에서는 산불 발생 positive 전체와 negative sampling을 결합한 학습 데이터를 사용하였다. 모델 간 공정 비교를 위한 benchmark 실험에서는 동일한 positive/negative 샘플링 조건을 적용하였고, 최종 후보 모델에 한해 연산 비용과 모델 특성을 고려하여 negative 샘플 수를 확장하였다.
```

### 12.3 성능 지표 선정 이유

```text
산불 위험도가 높더라도 실제 발화원이 존재하지 않으면 산불은 발생하지 않을 수 있으므로, 본 과제에서는 위험 후보군에서 산불이 발생하지 않은 경우를 단순한 오탐으로만 해석하기 어렵다. 따라서 Accuracy나 Precision만으로 모델을 평가하지 않고, 매일 상위 K개 고위험 후보군 안에 실제 산불 발생 격자가 포함되는지를 나타내는 일자별 Recall@Top-K와 무작위 선별 대비 효율을 나타내는 Lift@Top-K를 주요 지표로 사용하였다. 또한 불균형 데이터에서 양성 class ranking 성능을 확인하기 위해 PR-AUC를 함께 사용하였다.
```

### 12.4 최종 decision 산출 기준

```text
최종 제출용 decision은 일별 격자 위험도를 전신주 위치에 역매핑한 뒤, 전신주별 기간 위험도를 집계하여 산출하였다. 특정 전신주가 한 번이라도 매우 높은 위험에 노출되었는지와 반복적으로 고위험 구간에 포함되었는지를 함께 반영하기 위해 최대 위험도, 상위 5일 평균 위험도, 고위험일 비율을 결합한 pole_risk_score를 정의하였다. 최종적으로 pole_risk_score 상위 5% 전신주를 위험(1), 나머지를 비위험(0)으로 분류하였다.
```

---

## 13. 최종 체크리스트

### 13.1 모델링 실험 체크리스트

- [ ] Train / Validation / Test 기간 표기 통일
- [ ] 모델 간 공통 benchmark 샘플링 기준 통일
- [ ] 모델별 학습 샘플 수가 다를 경우 이유 명시
- [ ] DWI, FFDRI, P-FFDRI 단일지수 baseline 포함
- [ ] Logistic Regression은 baseline으로만 사용
- [ ] LightGBM, XGBoost, Random Forest는 비선형 보정 모델로 설명
- [ ] 500m, 1km, 2km target은 민감도 분석으로 정리
- [ ] 최종 대표 target을 선택했다면 선택 이유 명시

### 13.2 평가 지표 체크리스트

- [ ] Accuracy를 핵심 지표로 사용하지 않음
- [ ] Precision은 보조 지표로만 해석
- [ ] 일자별 Recall@Top-K를 주요 지표로 제시
- [ ] Lift@Top-K를 가능하면 포함
- [ ] PR-AUC를 불균형 데이터 ranking 지표로 사용
- [ ] ROC-AUC는 보조 지표로 사용
- [ ] threshold 기반 평가와 Top-K 평가의 차이를 설명

### 13.3 최종 decision 체크리스트

- [ ] 전신주 좌표를 100m grid_id에 매핑
- [ ] 일별 위험도를 전신주 위치에 역매핑
- [ ] 전신주별 여러 날짜 위험도를 집계
- [ ] `max`, `mean_top5`, `high_risk_day_ratio` 등 집계 기준 명시
- [ ] 상위 몇 %를 decision=1로 할지 고정
- [ ] decision=1 기준이 확정 예측이 아니라 우선점검 후보군 기준임을 명시

---

## 14. 최종 요약

최종 모델링 전략은 다음 한 문장으로 요약할 수 있다.

```text
본 과제는 산불 발생 확정 예측이 아니라 전력설비 주변 고위험 후보군 선별 문제로 정의하고, P-FFDRI를 강한 도메인 baseline으로 둔 뒤, Logistic Regression과 tree-based ensemble 모델을 통해 위험도 ranking을 보정하며, 최종적으로 일별 위험도를 전신주별 기간 위험도로 집계하여 decision 0/1을 산출한다.
```

이 전략을 따르면 모델링 결과가 다소 중구난방이더라도 최종 보고서에서는 다음 흐름으로 정리할 수 있다.

```text
문제 정의: 고위험 후보군 선별
모델 기준선: DWI, FFDRI, P-FFDRI
비교 모델: Logistic Regression, LightGBM, XGBoost, Random Forest
평가 기준: 일자별 Top-K, Lift, PR-AUC, Recall
제출 산출물: 전신주별 기간 위험도 상위 후보 decision=1
```
