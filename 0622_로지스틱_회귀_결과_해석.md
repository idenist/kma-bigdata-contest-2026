# 0622 로지스틱 회귀 결과 해석

## 0. 문서 목적

본 문서는 산불위험 예측 프로젝트의 모델링 전략 중 **선형 baseline 모델인 Logistic Regression** 실험 결과를 정리하기 위한 문서이다.

이번 실험의 목적은 최종 고성능 모델을 확정하는 것이 아니라, 다음 두 가지를 확인하는 데 있다.

```text
1. 기상·임상·지형·전력설비·P-FFDRI 계열 feature가 산불 발생 라벨을 어느 정도 선형적으로 설명하는지 확인
2. 이후 LightGBM, XGBoost, Random Forest 등 비선형 모델과 비교할 baseline 성능 확보
```

본 과제는 산불 발생 여부를 정확히 맞히는 일반적인 이진분류 문제라기보다, 제한된 점검 자원 하에서 **고위험 격자 또는 전신주 후보군을 우선순위화하는 ranking 문제**에 가깝다. 따라서 Accuracy 중심 평가보다는 **PR-AUC, Recall, F2-score, Recall@Top-K, Lift@Top-K**를 중심으로 결과를 해석한다.

---

## 1. 실험 설정

### 1.1 실행 명령어

```cmd
python scripts/modeling/train_logistic_regression.py --master-grid data/master_grid.parquet --grid-date-master output/final/final_feature_daily --fire-history data/산불발생이력.csv --out-dir outputs/logistic_1km_compact_pffdri_fulltest --radius-m 1000 --feature-set compact_full --neg-ratio 50 --topk 100 500 1000 5000 --eval-full-test
```

### 1.2 데이터 입력 경로

| 구분 | 경로 | 역할 |
|---|---|---|
| Master Grid | `data/master_grid.parquet` | 100m 격자 기준 정적 공간정보 |
| Daily Feature | `output/final/final_feature_daily` | 기상 원변수 + P-FFDRI 파생변수 포함 최종 일별 feature |
| Fire History | `data/산불발생이력.csv` | 산불 발생 이력 |
| Output | `outputs/logistic_1km_compact_pffdri_fulltest` | 모델 결과 저장 폴더 |

기존 1차 실험에서는 `--grid-date-master data/grid_date_master`를 입력으로 사용했기 때문에 `dwi`, `ffdri`, `pffdri` 등 핵심 파생변수가 누락되었다. 이번 실험에서는 `output/final/final_feature_daily`를 사용하여 P-FFDRI 계열 feature가 정상적으로 포함되었다.

### 1.3 학습·검증·평가 기간

| 구분 | 기간 | 역할 |
|---|---|---|
| Train | 2020~2022년 2~5월 | 모델 학습 |
| Validation | 2023년 2~5월 | threshold 선택 및 검증 |
| Test | 2024년 2~5월 | 최종 평가 |

무작위 분할 대신 시간 기준 분할을 사용하였다. 이는 실제 운영 상황에서 과거 데이터를 기반으로 미래 시점의 산불위험을 예측해야 하기 때문이다.

### 1.4 라벨 및 샘플링

| 항목 | 설정 |
|---|---|
| Target 반경 | 1km |
| Positive | 산불 발생점 기준 1km 이내 100m 격자 |
| Negative | Positive 수의 50배 샘플링 |
| 클래스 가중치 | `class_weight=balanced` |
| 평가 K | 100, 500, 1000, 5000 |

Positive 라벨은 산불 발생 좌표 기준 1km 반경 내 100m 격자에 부여하였다. 이는 발화점 좌표 오차와 산불 영향권을 함께 고려한 기준이다.

---

## 2. 사용 feature 확인

이번 실험에서 실제로 사용된 feature는 다음과 같다.

```text
ta_mean, ta_max, hm_mean, hm_min,
wind_ws_mean, wind_ws_max,
rn_day_mean, rn_day_max,
effective_humidity, rne, dwi, dwi_n, day_weight,
fmi_n, tmi_p, ywi, Da, Ws, Dr,
slope, elevation, aspect_sin, aspect_cos,
pei, pole_n, pole_count,
road_prox, river_far, nearest_road_dist, nearest_river_dist,
ffdri, pffdri,
month_num, day_num, dayofyear,
city_name
```

핵심적으로 다음 P-FFDRI 계열 변수가 포함되었다.

```text
effective_humidity
rne
dwi
dwi_n
day_weight
ffdri
pffdri
```

따라서 이번 실험은 단순 기상 원변수 모델이 아니라, **기상 원변수 + 공식 산불위험지수 계열 파생변수 + 임상·지형·전력설비 변수**를 결합한 compact-full 로지스틱 회귀 실험으로 해석할 수 있다.

---

## 3. Threshold 기반 평가 결과

### 3.1 Validation 결과

| 지표 | 값 |
|---|---:|
| ROC-AUC | 0.6901 |
| PR-AUC | 0.0427 |
| Threshold | 0.5126 |
| Precision | 0.0321 |
| Recall | 0.8133 |
| F2-score | 0.1387 |
| True Positive | 2,979 |
| False Positive | 89,794 |
| False Negative | 684 |
| True Negative | 93,356 |
| 예측 positive 비율 | 49.66% |
| 실제 positive 비율 | 1.96% |

Validation에서는 Recall이 0.8133으로 높게 나타났다. 즉, threshold 기준으로 위험 후보군을 넓게 잡으면 산불 positive 라벨 상당수를 포함할 수 있었다.

다만 예측 positive 비율이 49.66%로 매우 높다. 실제 positive 비율이 1.96%인 점을 고려하면, threshold 기반 분류는 지나치게 많은 격자를 위험 후보군으로 분류하고 있다.

따라서 이 결과는 “산불 발생 격자를 놓치지 않기 위해 넓은 후보군을 형성할 수 있다”는 의미는 있지만, 운영형 의사결정에서 그대로 사용하기에는 후보군 규모가 너무 크다.

### 3.2 Test sample 결과

| 지표 | 값 |
|---|---:|
| ROC-AUC | 0.6866 |
| PR-AUC | 0.0510 |
| Threshold | 0.5126 |
| Precision | 0.0436 |
| Recall | 0.4457 |
| F2-score | 0.1567 |
| True Positive | 648 |
| False Positive | 14,210 |
| False Negative | 806 |
| True Negative | 58,490 |
| 예측 positive 비율 | 20.04% |
| 실제 positive 비율 | 1.96% |

Test sample에서는 Validation에서 선택한 threshold를 그대로 적용했을 때 Recall이 0.4457로 하락하였다. Validation 대비 Recall은 낮아졌지만, F2-score는 0.1567로 오히려 소폭 높아졌다.

이 결과는 threshold가 연도별 score 분포에 민감하다는 점을 보여준다. 특히 2023년에서 고른 threshold가 2024년에도 동일하게 최적이라고 보기 어렵다.

따라서 로지스틱 회귀 모델은 threshold 기반 확정 분류보다는 **위험도 순위화 baseline**으로 해석하는 것이 적절하다.

---

## 4. 샘플 Top-K 평가 결과

### 4.1 Validation Top-K

| K | 일수 | Mean Recall@K | Weighted Recall@K | Mean Precision@K | Mean Lift@K |
|---:|---:|---:|---:|---:|---:|
| 100 | 28 | 0.1024 | 0.0729 | 0.0954 | 1.6500 |
| 500 | 28 | 0.4543 | 0.3522 | 0.0921 | 1.4829 |
| 1000 | 28 | 0.6835 | 0.5990 | 0.0831 | 1.1796 |
| 5000 | 28 | 1.0000 | 1.0000 | 0.0760 | 1.0000 |

### 4.2 Test sample Top-K

| K | 일수 | Mean Recall@K | Weighted Recall@K | Mean Precision@K | Mean Lift@K |
|---:|---:|---:|---:|---:|---:|
| 100 | 10 | 0.1478 | 0.1265 | 0.1840 | 1.3085 |
| 500 | 10 | 0.5779 | 0.4883 | 0.1648 | 1.4556 |
| 1000 | 10 | 0.6911 | 0.5667 | 0.1346 | 1.1106 |
| 5000 | 10 | 1.0000 | 1.0000 | 0.1342 | 1.0000 |

샘플 평가에서는 Top-500 또는 Top-1000 기준으로 비교적 높은 Recall@K가 나타났다. Test sample 기준 Top-500에서 weighted Recall@K는 0.4883, Top-1000에서는 0.5667이다.

그러나 이 결과는 negative sampling된 샘플 데이터에서의 평가이므로, 실제 운영 성능으로 직접 해석하면 안 된다. 샘플 데이터는 positive 1개당 negative 50개만 포함하므로, 전체 격자 모집단에서 매일 상위 K개를 고르는 상황보다 훨씬 쉬운 평가 조건이다.

따라서 샘플 Top-K 결과는 모델 간 공정 benchmark 비교용으로 사용하고, 운영형 성능은 전체 2024년 모집단 Top-K 평가를 기준으로 해석하는 것이 적절하다.

---

## 5. 2024년 전체 모집단 Top-K 평가 결과

이번 실험의 핵심 평가는 `--eval-full-test` 옵션으로 수행한 2024년 전체 격자 모집단 Top-K 평가이다.

### 5.1 Full Test Top-K 결과

| K | 일수 | Mean Recall@K | Weighted Recall@K | Mean Precision@K | Mean Lift@K |
|---:|---:|---:|---:|---:|---:|
| 100 | 10 | 0.0018 | 0.0007 | 0.0010 | 4.7895 |
| 500 | 10 | 0.0160 | 0.0083 | 0.0024 | 8.7607 |
| 1000 | 10 | 0.0221 | 0.0131 | 0.0019 | 6.0242 |
| 5000 | 10 | 0.0789 | 0.0832 | 0.0024 | 4.3103 |

해석하면 다음과 같다.

```text
하루 전체 약 273,001개 격자 중 상위 5,000개를 선택했을 때,
2024년 positive label의 약 8.32%를 포착하였다.
```

Top-5000은 전체 격자의 약 1.83%에 해당한다. 이 1.83% 후보군 안에 positive의 8.32%가 포함되었으므로 무작위 선별보다는 효율적이다. Top-500 기준 mean Lift@K가 8.7607로 나타난 점도 모델이 일부 고위험 격자를 상위권으로 끌어올리는 능력이 있음을 보여준다.

다만 절대적인 Recall@Top-K는 아직 낮다. 특히 Top-5000에서도 전체 positive의 약 8.32%만 포착하므로, 로지스틱 회귀 단독 모델을 최종 운영 모델로 사용하기에는 한계가 있다.

---

## 6. 이전 raw weather 모델과의 비교

1차 실험은 `data/grid_date_master`를 입력으로 사용하여 P-FFDRI 계열 파생변수가 빠진 상태에서 수행되었다. 해당 결과는 “기상 원변수 + 정적 공간변수 기반 baseline”으로 해석할 수 있다.

이번 실험은 `output/final/final_feature_daily`를 입력으로 사용하여 `effective_humidity`, `rne`, `dwi`, `ffdri`, `pffdri` 등이 포함된 상태에서 수행되었다.

### 6.1 Full Test Weighted Recall@K 비교

| K | Raw weather baseline | P-FFDRI 포함 모델 | 변화 |
|---:|---:|---:|---:|
| 100 | 0.0000 | 0.0007 | 소폭 개선 |
| 500 | 0.0021 | 0.0083 | 약 4.0배 개선 |
| 1000 | 0.0062 | 0.0131 | 약 2.1배 개선 |
| 5000 | 0.0371 | 0.0832 | 약 2.2배 개선 |

### 6.2 Full Test Lift@K 비교

| K | Raw weather baseline | P-FFDRI 포함 모델 | 변화 |
|---:|---:|---:|---:|
| 100 | 0.0000 | 4.7895 | 개선 |
| 500 | 0.8231 | 8.7607 | 크게 개선 |
| 1000 | 1.2347 | 6.0242 | 개선 |
| 5000 | 1.7658 | 4.3103 | 개선 |

P-FFDRI 계열 feature를 포함하자 전체 모집단 Top-K 성능은 확실히 개선되었다. 특히 Top-5000 기준 weighted Recall@K가 0.0371에서 0.0832로 상승했다.

실제 hit 수로 보면 다음과 같다.

```text
2024년 전체 positive 수: 1,454개
Raw weather baseline Top-5000 hit: 54개
P-FFDRI 포함 모델 Top-5000 hit: 121개
```

따라서 P-FFDRI 계열 파생변수는 로지스틱 회귀 모델의 전체 모집단 ranking 성능을 개선하는 데 기여했다고 볼 수 있다.

---

## 7. 일자별 Full Test Top-K 해석

전체 Top-K 평균만 보면 개선이 확인되지만, 일자별 세부 결과를 보면 성능이 일부 날짜에 집중되어 있다.

### 7.1 주요 포착 날짜

| 날짜 | Positive 수 | Top-100 hit | Top-500 hit | Top-1000 hit | Top-5000 hit | Top-5000 Recall |
|---|---:|---:|---:|---:|---:|---:|
| 2024-04-07 | 502 | 0 | 0 | 0 | 31 | 0.0618 |
| 2024-04-12 | 199 | 0 | 4 | 9 | 68 | 0.3417 |
| 2024-05-24 | 57 | 1 | 8 | 10 | 22 | 0.3860 |

Top-5000 기준으로는 2024-04-12와 2024-05-24에서 비교적 높은 Recall이 나타났다. 특히 2024-04-12는 Top-5000에서 199개 positive 중 68개를 포착하여 Recall@5000이 0.3417이었다. 2024-05-24는 positive 수가 57개로 적지만 Top-5000에서 22개를 포착해 Recall@5000이 0.3860이었다.

### 7.2 포착하지 못한 날짜

다음 날짜에서는 Top-5000 기준으로도 positive를 포착하지 못했다.

```text
2024-03-15
2024-04-01
2024-04-02
2024-04-13
2024-04-14
2024-04-19
2024-05-04
```

즉, 모델은 일부 산불 발생일에서는 산불 발생 격자를 상위 후보군으로 끌어올렸지만, 다수의 발생일에서는 충분히 포착하지 못했다.

이는 로지스틱 회귀가 특정 고위험 기상 패턴에는 반응하지만, 산불 발생 위치의 지역·지형·기상 상호작용을 안정적으로 포착하기에는 한계가 있음을 의미한다.

---

## 8. 로지스틱 회귀 계수 해석

### 8.1 주요 계수

| Feature | 계수 | 해석 |
|---|---:|---|
| `dayofyear` | -7.2592 | 날짜 효과가 크게 반영됨 |
| `month_num` | 6.9665 | 월별 계절성이 크게 반영됨 |
| `city_name_속초시` | -5.4012 | 특정 지역 효과가 크게 반영됨 |
| `ffdri` | -2.4267 | 다중공선성 영향 가능성이 큼 |
| `effective_humidity` | -1.2798 | 실효습도가 높을수록 위험 낮아지는 방향 |
| `rn_day_max` | -1.0952 | 강수량이 많을수록 위험 낮아지는 방향 |
| `fmi_n` | 0.6845 | 임상 위험이 높을수록 위험 증가 방향 |
| `pffdri` | 0.5934 | P-FFDRI가 높을수록 위험 증가 방향 |
| `dwi`, `dwi_n` | 0.5179 | 기상위험지수가 높을수록 위험 증가 방향 |
| `rne` | 0.4553 | 강우효과 변수의 방향성은 후속 확인 필요 |
| `ta_max` | 0.4298 | 최고기온이 높을수록 위험 증가 방향 |
| `hm_min` | -0.3488 | 최저습도가 낮을수록 위험 증가 방향 |
| `wind_ws_max` | 0.1890 | 최대풍속이 높을수록 위험 증가 방향 |

### 8.2 상식적인 방향

다음 변수들은 도메인적으로 비교적 자연스러운 방향을 보인다.

```text
effective_humidity 음수
rn_day_mean, rn_day_max 음수
hm_min 음수
ta_mean, ta_max 양수
wind_ws_max 양수
dwi, dwi_n 양수
pffdri 양수
fmi_n 양수
```

해석하면 다음과 같다.

```text
습도와 강수량이 높을수록 산불 위험은 낮아지고,
기온·최대풍속·기상위험지수·P-FFDRI·임상위험지수가 높을수록 산불 위험은 높아지는 방향으로 학습되었다.
```

### 8.3 주의할 계수

`ffdri` 계수가 음수로 나타난 점은 단순히 “FFDRI가 높을수록 산불 위험이 낮아진다”로 해석하면 안 된다.

이번 feature set에는 다음처럼 서로 강하게 관련된 변수들이 동시에 포함되어 있다.

```text
effective_humidity
rne
dwi
dwi_n
ffdri
pffdri
day_weight
```

이 변수들은 모두 산불위험지수 산출 과정에서 연결되어 있기 때문에 다중공선성이 발생할 수 있다. 따라서 로지스틱 회귀의 개별 계수는 독립적인 인과효과가 아니라, **표준화된 입력 변수 조합 내에서의 선형 기여 방향을 참고하는 보조 지표**로 해석해야 한다.

---

## 9. 종합 해석

이번 실험의 핵심 결론은 다음과 같다.

```text
P-FFDRI 계열 파생변수를 포함한 Logistic Regression은
원천 기상변수 기반 baseline보다 2024년 전체 모집단 Top-K 성능을 개선하였다.
```

특히 Top-5000 기준 weighted Recall@K가 0.0371에서 0.0832로 상승했고, hit 수는 54개에서 121개로 증가하였다. 이는 도메인 기반 위험지수 feature가 실제 산불 발생 격자의 ranking 성능 향상에 기여했음을 보여준다.

그러나 절대 성능은 아직 낮다. 2024년 전체 격자 모집단에서 Top-5000을 선택해도 positive의 약 8.32%만 포착하였다. 또한 일자별 성능 편차가 크고, 다수의 발생일에서는 Top-5000 내 positive를 포착하지 못했다.

따라서 Logistic Regression은 최종 모델이라기보다 다음 역할로 사용하는 것이 적절하다.

```text
1. 선형 baseline 성능 확인
2. P-FFDRI 계열 파생변수 추가 효과 검증
3. 변수 방향성에 대한 보조 해석 제공
4. LightGBM/XGBoost 등 비선형 모델과 비교하기 위한 기준선
```

---

## 10. 보고서용 문장

최종 보고서에는 다음과 같이 정리할 수 있다.

```text
Logistic Regression은 기상·임상·지형·전력설비 변수와 P-FFDRI 계열 파생변수를 결합한 선형 baseline 모델로 사용하였다. 샘플링된 benchmark 데이터에서는 ROC-AUC 0.69, PR-AUC 0.051 수준의 ranking 성능을 보였으며, 2024년 전체 격자 모집단 Top-K 평가에서는 Top-5000 기준 weighted Recall@K 0.0832, Lift@K 4.31을 기록하였다.

이는 원천 기상변수 중심 baseline 대비 개선된 결과로, P-FFDRI 계열 도메인 파생변수가 고위험 격자를 상위 후보군으로 끌어올리는 데 기여했음을 의미한다. 다만 전체 모집단 기준 절대 Recall@Top-K는 아직 낮고 일자별 편차가 커서, Logistic Regression은 최종 모델보다는 선형 baseline 및 설명 가능성 확인용 모델로 활용하였다. 이후 비선형 tree-based ensemble 모델을 통해 변수 간 상호작용과 지역별 위험 구조를 추가로 학습할 필요가 있다.
```

---

## 11. 다음 실험 방향

### 11.1 LightGBM compact-full 실험

로지스틱 회귀는 선형 모델이므로 변수 간 비선형 상호작용을 충분히 반영하기 어렵다. 다음 단계에서는 동일한 train/validation/test 분할과 동일한 sampling 조건에서 LightGBM을 학습하여 비교하는 것이 좋다.

비교 기준은 다음과 같다.

```text
Raw weather Logistic Regression
→ P-FFDRI Logistic Regression
→ P-FFDRI LightGBM
```

### 11.2 Feature set 비교

추가로 다음 feature set 실험을 수행하면 보고서 설득력이 높아진다.

| 실험명 | Feature 구성 | 목적 |
|---|---|---|
| Index-only | `dwi`, `ffdri`, `pffdri` | 위험지수 단독 성능 확인 |
| Raw weather + static | 기상 원변수 + 임상·지형·전력설비 | 원천 변수 baseline |
| Compact-full | 기상 + P-FFDRI + 임상·지형·전력설비 | 현재 실험 |
| No-index | `ffdri`, `pffdri` 제외 | 위험지수 추가 효과 검증 |

### 11.3 Threshold 대신 Top-K 중심 해석

이번 결과에서 threshold 기반 평가는 예측 positive 비율이 지나치게 커지는 문제가 있었다. 따라서 최종 보고서에서는 threshold 기반 성능을 보조 지표로 제시하고, 운영형 평가는 Top-K 중심으로 해석하는 것이 적절하다.

---

## 12. 최종 요약

```text
이번 로지스틱 회귀 실험은 성공적인 최종 모델이라기보다,
P-FFDRI feature의 효과를 확인한 선형 baseline 실험이다.

P-FFDRI 포함 후 전체 모집단 Top-K 성능은 개선되었지만,
절대 Recall은 낮아 비선형 보정 모델이 필요하다.
```

최종 판단은 다음과 같다.

| 항목 | 판단 |
|---|---|
| 로지스틱 회귀 baseline 필요성 | 있음 |
| P-FFDRI feature 추가 효과 | 있음 |
| threshold 기반 최종 decision 사용 | 부적절 |
| Top-K ranking baseline 활용 | 가능 |
| 최종 모델 채택 | 비추천 |
| 다음 단계 | LightGBM/XGBoost 등 비선형 모델 비교 |
