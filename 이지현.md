# 0621 LightGBM 실험 보고서

## 0. 결론 요약

본 실험은 P-FFDRI 프로젝트의 500m 격자 feature를 이용해 LightGBM 분류 모델을 학습시키고, train 2020~2022 / valid 2023(threshold 튜닝) / test 2024(전체 모집단) 구조로, F2-score·혼동행렬·일자별 Top-K를 포함한 평가를 수행한 결과다.

핵심 결론은 다음과 같다.

```text
1. LightGBM은 Recall(91.7%)은 매우 높지만 Precision(0.00085%)이 극히 낮다.
2. ROC-AUC 기준으로는 LightGBM(0.813)이 단일 위험지수 3종(DWI 0.888 / FFDRI 0.897 / P-FFDRI 0.899)
   보다 여전히 낮았다.
3. 일자별 Top-10,000 기준 실제 포착 화재 건수는 P-FFDRI(8건) > FFDRI(6건) > LightGBM(5건) > DWI(0건) 순이었다.
4. feature 병합 과정에서 실제 산불 양성의 약 29.6%가 누락되는 것을 확인했다.
```

평가 인프라(3-way split, threshold 탐색, 일자별 Top-K, 전체 모집단 평가)를 갖췄으므로, 이후 해상도(1km/2km) 및 하이퍼파라미터 튜닝을 추가하면 비교가 더 정밀해질 것으로 본다.

---

## 1. 실험 목적

- 최종 feature(`final_feature_daily`)와 산불 라벨(`target_fire_daily_500m`)을 이용해 LightGBM 분류 모델의 baseline 성능을 확인한다.
- 시간 기준 3-way 분할, threshold 튜닝, F2-score, 일자별 Top-K, 전체 모집단 평가라는 엄격한 방법론을 적용해 신뢰할 수 있는 성능 수치를 산출한다.
- 단일 위험지수(DWI, FFDRI, P-FFDRI)와 LightGBM의 성능을 같은 기준으로 비교한다.
- feature 병합 과정에서의 positive 누락 여부를 직접 검증한다.

---

## 2. 사용 데이터 및 전처리

### 2.1 입력 데이터

| 데이터 | 경로 | 의미 |
|---|---|---|
| 설명변수(X) | `output/final/final_feature_daily/` | 날짜×격자(500m) 단위 기상·연료·지형·전력설비·위험지수. 197,106,722행, 273,001격자, 2020-02-01~2025-05-31 |
| 정답(y) | `output/target/target_fire_daily_500m.parquet` | 500m 기준 실제 산불 발생 격자-일자. 1,918행 |
| 학습용 표본(train/valid) | `output/eda/eda_sample_500m.parquet` | X+y 결합 후 양성 전체 + 음성 60배 샘플링. 81,222행 |

### 2.2 Positive 누락 진단

feature 병합 과정에서 양성 라벨이 얼마나 유지되는지 직접 검증했다.

| 구분 | 값 |
|---|---:|
| `target_fire_daily_500m.parquet` 원본 행 수 | 1,918 |
| `final_feature_daily`와 INNER JOIN 후 남은 행 수 | 1,350 |
| 누락 | 568 (29.6%) |

따라서 본 실험 및 기존 EDA에서 "양성 1,350건"이라 표기한 수치는 **전체 산불 이력이 아니라 feature 병합에서 살아남은 건수**임을 명시해야 한다. 누락 원인은 좌표 변환·격자 경계·날짜 매칭 단계에서 추가로 추적이 필요하다.

### 2.3 제외 컬럼 / 범주형 처리

leakage 컬럼(`fire_label` 외 12개), 식별자(`grid_id`, `date`)는 제외했고, `month`, `is_forest`, `forest_type_code`, `age_class_code`, `tree_height_code`는 범주형으로 처리했다. 최종 사용 feature는 43개다.

---

## 3. 검증 설계

산불 데이터는 시공간 자기상관이 강하므로 무작위 분할 대신 **3-way 시간 분할**을 적용했다 (미래 예측 상황을 모사).

| 구분 | 기간 | 역할 | 행 수(표본) | 양성 |
|---|---|---|---:|---:|
| Train | 2020~2022 | 모델 학습 | 40,839 | 917 |
| Valid | 2023 | Threshold 탐색 | 13,622 | 313 |
| Test | 2024 | 최종 평가 | **33,033,121 (전체 모집단)** | 120 |

Test(2024)는 표본이 아니라 **`final_feature_daily`의 2024년 전체 행을 청크 단위로 읽어 예측만 수행**했다(학습이 아닌 추론이므로 메모리 누적·발산 문제가 없다).

### Threshold 선택

valid(2023)에서 **Recall ≥ 0.5를 만족하는 후보 중 Precision이 가장 높은 threshold**를 선택하는 방식을 적용했다.

```text
선택된 threshold = 0.000385
```

이 값이 매우 낮게 나온 이유는, threshold를 탐색한 표본의 음성 비율과 실제 전체 모집단의 음성 비율 차이가 매우 크기 때문이다 — 9장에서 자세히 다룬다.

---

## 4. 모델 설정

```python
LGBMClassifier(
    objective="binary",
    n_estimators=500,
    learning_rate=0.03,
    num_leaves=31,
    max_depth=-1,
    min_child_samples=50,
    subsample=0.8,
    colsample_bytree=0.8,
    class_weight="balanced",
    random_state=42,
    n_jobs=-1,
)
```

하이퍼파라미터 튜닝은 아직 수행하지 않았다 (1차 baseline 권장값을 그대로 사용).

---

## 5. 결과

### 5.1 최종 평가 — 2024년 전체 모집단 기준

| 지표 | 값 |
|---|---:|
| n (전체 모집단) | 33,033,121 |
| 실제 양성 | 120 |
| ROC-AUC | 0.8132 |
| PR-AUC | 0.000011 |
| Recall | 0.9167 |
| Precision | 0.00000852 |
| F1-score | 0.0000170 |
| F2-score | 0.0000426 |

### 혼동행렬 (threshold=0.000385)

| | 예측 양성 | 예측 음성 |
|---|---:|---:|
| **실제 양성** | TP = 110 | FN = 10 |
| **실제 음성** | FP = 12,905,144 | TN = 20,127,857 |

실제 산불 120건 중 110건을 잡아냈지만(Recall 91.7%), 그 대가로 전체 격자-일자 조합의 약 **39%(1,290만 건)를 오탐(FP)** 했다. **Recall은 높지만 Precision이 극히 낮아 단독 의사결정에는 부적합**하며, 고위험 후보지를 넓게 선별하는 보조 지표로 활용하는 것이 적절하다.

### 일자별 Top-K 평가 (매일 상위 K개 선정 후 누적 포착 건수 집계)

| Top-K | 추천 격자 수(누적) | 실제 산불 포착 수 | Recall@K |
|---|---:|---:|---:|
| Top-500 | 60,500 | 0 | 0.00% |
| Top-1,000 | 121,000 | 0 | 0.00% |
| Top-5,000 | 605,000 | 1 | 0.83% |
| Top-10,000 | 1,210,000 | 5 | 4.17% |

### 5.2 (참고) 초기 baseline — 표본 기준 글로벌 랭킹 방식

본 실험 초기에는 표본(81,222행)의 2024년 분량(13,529행)만 떼어 "전체 기간을 한 번에 랭킹"하는 방식으로 빠르게 baseline을 확인했다. 이 결과는 **평가 범위·방식이 5.1과 달라 직접 비교 불가**하지만, 참고용으로 남긴다.

| 모델 | ROC-AUC | PR-AUC | Recall@Top10%(글로벌) | Recall@Top20%(글로벌) |
|---|---:|---:|---:|---:|
| LightGBM | 0.841 | 0.038 | 38.3% | 66.7% |
| Logistic Regression | 0.835 | 0.030 | 28.3% | 64.2% |

5.1과 5.2의 숫자가 크게 다른 이유는 모델이 달라져서가 아니라 **평가 모집단의 규모와 방식이 다르기 때문**이다(5.2는 음성을 60배로 줄인 표본 1.3만 행 기준, 5.1은 전체 모집단 3,300만 행 + 일자별 Top-K 기준). 대표값으로는 **5.1을 사용**해야 한다.

---

## 6. 단일 위험지수(DWI/FFDRI/P-FFDRI) 동일 기준 비교

LightGBM과 동일하게 2024년 전체 모집단(33,033,121행) + 일자별 Top-K 방식으로 단일 지수를 재평가했다.

| 지수 | ROC-AUC | Top500 포착 | Top1,000 포착 | Top5,000 포착 | Top10,000 포착 |
|---|---:|---:|---:|---:|---:|
| DWI | 0.8884 | 0 | 0 | 0 | 0 (0.0%) |
| FFDRI | 0.8968 | 2 (1.67%) | 2 (1.67%) | 2 (1.67%) | 6 (5.0%) |
| **P-FFDRI** | **0.8992** | 0 | 0 | 3 (2.5%) | **8 (6.67%)** |
| LightGBM | 0.8132 | 0 | 0 | 1 (0.83%) | 5 (4.17%) |

### 해석

ROC-AUC·Top-10,000 포착 건수 모두 **P-FFDRI > FFDRI > LightGBM > DWI** 순이다. 표본 기준 평가(§5.2)에서는 LightGBM·DWI가 상대적으로 좋아 보였지만, **전체 모집단 + 일자별 Top-K로 재평가하면 P-FFDRI의 우위가 더 뚜렷해진다.** 평가 범위를 표본에서 전체 모집단으로 확장할수록, 사람이 설계한 위험지수의 안정성이 더 잘 드러난다는 것이 이번 실험의 핵심 발견이다.

---

## 7. Feature Importance

| 순위 | 변수 | 의미 |
|---:|---|---|
| 1 | `effective_humidity` | 실효습도(누적 건조도) |
| 2 | `hm_mean` | 평균습도 |
| 3 | `wind_ws_mean` | 평균풍속 |
| 4 | `month` | 월(계절성) |
| 5 | `ta_max` | 최고기온 |
| 6 | `wind_wd_cos_mean` | 풍향(cos 성분) |
| 7 | `hm_min` | 최저습도 |
| 8 | `elevation` | 고도 |
| 9 | `wind_ws_max` | 최대풍속 |
| 10 | `ta_mean` | 평균기온 |

**기상(특히 습도·풍속) 변수가 압도적으로 중요**하다는 것이 이번 LightGBM 실험의 결론이다. 이는 P-FFDRI 설계에 쓰인 변수들과 방향이 일치하며, 모델이 합리적인 패턴을 학습했음을 보여준다.

---

## 8. 한계 및 주의사항

```text
1. threshold(0.000385)는 표본(2023년 valid, 13,622행)에서 탐색했는데,
   실제 평가는 전체 모집단(2024년, 33,033,121행)에서 이루어졌다.
   표본의 음성 비율(약 9:1)과 전체 모집단의 음성 비율(약 275,000:1)이 크게 달라서,
   표본에서 "적당히 엄격해 보이는" threshold가 전체 모집단에서는
   지나치게 관대한 기준이 되어 FP가 폭증했다 (1,290만 건).
   → 다음 실험에서는 threshold를 표본이 아니라 전체 모집단(또는 그에 가까운 대규모 검증셋) 기준으로 다시 탐색해야 한다.
2. feature 병합 과정에서 양성의 29.6%가 누락된다 (§2.2). 따라서 모든 평가는
   "원본 전체 산불"이 아니라 "feature가 존재하는 격자-일자 조합" 기준으로 해석해야 한다.
3. 본 실험은 500m 해상도, 하이퍼파라미터 튜닝 없는 1차 baseline이다.
   1km/2km 비교, 하이퍼파라미터 튜닝은 아직 수행하지 않았다.
4. PR-AUC, Precision 등 절대값이 매우 작게 나오는 것은 모델 결함이 아니라
   양성 비율 자체가 0.0004%(120/33,033,121) 수준으로 극단적으로 낮기 때문이다.
```

---

## 9. 핵심 메시지

### 메시지 1. 전체 모집단·일자별 Top-K로 재평가해도 단일 위험지수가 우위

```text
DWI·FFDRI·P-FFDRI를 LightGBM과 동일한 기준(2024년 전체 모집단 3,303만 행, 일자별 Top-K)으로 평가한 결과, P-FFDRI가 ROC-AUC 0.899로 가장 우수했고 Top-10,000 기준 실제 산불 8건을 포착해 LightGBM(5건)보다 많았다.
```

### 메시지 2. LightGBM은 Recall은 높지만 Precision이 극히 낮다

```text
LightGBM은 실제 산불 120건 중 110건(91.7%)을 포착했지만, 그 대가로 전체 모집단의 약 39%를 오탐했다. 산불 발생을 확정 예측하는 모델이 아니라 고위험 후보지를 넓게 선별하는 보조 모델로 활용해야 함을 보여준다.
```

### 메시지 3. Feature 병합 시 양성 약 30% 누락 — 평가 해석에 주의 필요

```text
target 원본 1,918건 중 final_feature_daily와 결합 후 1,350건만 남아, 약 29.6%가 누락되었다. 병합 로직 점검이 필요하다.
```

---

## 10. 다음 단계 제안

| 작업 | 내용 |
|---|---|
| Threshold 재탐색 | 표본이 아닌 전체 모집단(또는 대규모 검증셋) 기준으로 threshold 재선택 |
| 1km / 2km 비교 | 동일한 3-way 분할·threshold·일자별 Top-K 방법론을 1km, 2km 라벨에도 적용 |
| 하이퍼파라미터 튜닝 | `num_leaves`, `min_child_samples`, `learning_rate` 등을 valid(2023) 기준으로 탐색 |
| 다른 트리 모델 비교 | Random Forest, XGBoost를 동일한 조건에서 비교 |
| Feature 병합 로직 점검 | positive 29.6% 누락 원인 추적 (좌표 변환, 격자 경계, 날짜 매칭 등) |

---

## 11. 저장된 산출물

| 파일 | 경로 |
|---|---|
| 학습/평가 코드(최종) | `modeling/src/lightgbm_final_report.py` |
| 학습 코드(초기 baseline, 표본 기준) | `modeling/src/lightgbm_baseline.py` |
| 저장된 모델 | `modeling/src/lightgbm_final_model.txt` |
| 단일지수 일자별 Top-K 결과 | `output/eda_guidecheck/single_index_daily_topk_2024full.csv` |
| 학습용 표본 | `output/eda/eda_sample_500m.parquet` |
